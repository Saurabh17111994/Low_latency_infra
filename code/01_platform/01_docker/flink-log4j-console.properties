# P8.2 (tracker 14) override: keep the console appender AND write to a file
# so the otel-collector filelog receiver can ship flink_logs to OpenObserve.
# The flink bin scripts set -Dlog.file=<FLINK_LOG_DIR>/<role>.log (the
# docker entrypoint defaults FLINK_LOG_DIR to /opt/flink/log, the shared
# flink-logs volume). Console output stays intact for docker logs.
rootLogger.level = INFO
rootLogger.appenderRef.console.ref = ConsoleAppender
rootLogger.appenderRef.rolling.ref = RollingFileAppender

appender.console.name = ConsoleAppender
appender.console.type = CONSOLE
appender.console.target = SYSTEM_ERR
appender.console.layout.type = PatternLayout
appender.console.layout.pattern = %d{yyyy-MM-dd HH:mm:ss,SSS} %-5p %-60c %x - %m%n

appender.rolling.name = RollingFileAppender
appender.rolling.type = RollingFile
# P5-027: default the lookup — without -Dlog.file (IDE run, custom launcher,
# entrypoint mismatch) the raw lookup stays unresolved and the appender
# writes a literal file named "log.file" in the cwd, which never ships.
# Default = the entrypoint's FLINK_LOG_DIR (/opt/flink/log, shared volume).
appender.rolling.fileName = ${sys:log.file:-/opt/flink/log/flink.log}
# P5-028: keep the .log suffix on rotated files — the collector's
# filelog/flink receiver includes /data/flink/logs/*.log ONLY; the old
# "<role>.log.%i" pattern fell out of the glob at the first 10MB rollover
# and everything in the rolled file silently stopped shipping.
appender.rolling.filePattern = ${sys:log.file:-/opt/flink/log/flink.log}.%i.log
appender.rolling.layout.type = PatternLayout
appender.rolling.layout.pattern = %d{yyyy-MM-dd HH:mm:ss,SSS} %-5p %-60c %x - %m%n
appender.rolling.policies.type = Policies
appender.rolling.policies.size.type = SizeBasedTriggeringPolicy
appender.rolling.policies.size.size = 10MB
appender.rolling.strategy.type = DefaultRolloverStrategy
appender.rolling.strategy.max = 10

logger.akka.name = akka
logger.akka.level = WARN
logger.kafka.name= org.apache.kafka
logger.kafka.level = WARN
logger.hadoop.name = org.apache.hadoop
logger.hadoop.level = WARN
logger.zookeeper.name = org.apache.zookeeper
logger.zookeeper.level = WARN
