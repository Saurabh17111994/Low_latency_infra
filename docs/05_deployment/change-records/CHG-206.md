# CHG-206 — Wave 38 commit 2: baseline JOB_ID gate / G24 / handoff (P6-210/211/212-env)

## Changes
- P6-210 (`stage-a2-baseline.sh`): 8+-hex JOB_ID gate between submit and the
  wait loop (defense-in-depth: `pipeline_submit_job` already fails closed on
  empty parse; pinned by source-text+order test since the path is
  unreachable via real submit).
- P6-211 (`pipeline-lib.sh` new `pipeline_g24_rocksdb_check`, baseline calls
  it): mount-source check (absent/bind fail BEFORE the 60s wait) + exact
  `job_${JOB_ID}_op_*` dir wait with `"${COMPOSE[@]}"` (fixes the wave-17
  bare-`docker` regression AND the missing layer-1 class).
- P6-212 env half (baseline handoff): forwards RATE_HZ / MIN_UPTIME_S /
  STATE_BACKEND / SOURCE_RATE_FLOOR_PCT. Manifest-write half deferred to
  commit 3 — `gate_manifest_add` exists nowhere yet; commit 3 creates the
  helper + the baseline call site.
- `tests/stubs/wave38_docker.py`: STUB_DOCKER_PS / _INSPECT / _EXEC_RC knobs
  (defaults = old always-0 behavior; the P6-562 teardown tests unaffected).

## Verification
- `bash -n` both scripts; 9/9 pass (0.35s); the 5 new tests fail vs parent
  commit (failing-first; the 4 commit-1 tests pass — already landed).
- Hermetic: stub docker/sleep everywhere; no cluster touch (TM uptime unchanged).

```text
change_record_id: CHG-206
scope: gate-behavior
owner: saurabh
date: 2026-09-17
affected_artifacts: code/01_platform/04_scripts/stage-a2-baseline.sh, code/01_platform/04_scripts/pipeline-lib.sh, code/01_platform/04_scripts/tests/test_stage_a2_baseline_wave38.py, code/01_platform/04_scripts/tests/stubs/wave38_docker.py
compatibility_class: COMPATIBLE
savepoint_impact: none — soak/baseline tooling only; no Flink job state, savepoint, or schema touched
test_updates: +5 cases (failing-first vs the parent commit) plus STUB_DOCKER_PS/_INSPECT/_EXEC_RC knobs in the wave-38 docker stub (defaults keep the old always-0 behavior)
rollback_behavior: revert the commit. The baseline loses the JOB_ID gate, G24 returns to the wave-17 bare-docker form, and the handoff stops forwarding the four env keys
plan_tasks: none — wave-38 scope doc was never committed; findings P6-210/211/212 from the p6 audit §10
```
