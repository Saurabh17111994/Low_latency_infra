# CHG-205 — Wave 38 commit 1: stage-a2-baseline header guards (P6-552/553/554)

Findings: P6-552 (run-dir collision), P6-553 (unvalidated uptime gate),
P6-554 (divergent bring-up recipe). Approved scope:
docs/plans/2026-09-17-wave38-stage-capture-baseline.md (in stash@{0}).

## Changes (code/01_platform/04_scripts/stage-a2-baseline.sh)
- P6-552: PHASE_OUT gains `-$$` suffix + `mkdir -p || fatal`.
- P6-553: MIN_UPTIME_S integer validation; `[ -r /proc/uptime ]` fail-closed;
  uptime parse validation (fixes the `[ -lt ]`-error gate-skip class).
- P6-554: bring-up uses lib `"${COMPOSE[@]}" up -d ...`; COMPOSE_DIR removed
  (dead after the switch).

## Deviation (verified live, blocks the audit's literal fix)
P6-468 (wave 17) made $COMPOSE an ARRAY. Scalar `$COMPOSE` = element 0 =
bare `docker`. The audit's literal `$COMPOSE up -d` would run `docker up`
(no such command); baseline L121 `$COMPOSE exec` (G24) has been broken since
wave 17 (`docker exec -T flink-taskmanager` fails; proven against the live
cluster). Both this fix and commit 2 (P6-211) use `"${COMPOSE[@]}"`.

## Incident during verification (own goal, no lasting damage)
Failing-first run vs OLD code: the abc-MIN_UPTIME case (no stubs) skipped
the old gate, reached real curl (JM up) and entered live preflight, which
restarted the TM (B3) before the 120s pytest timeout killed it. No purge /
submit happened (pre-B5 wait). Babysitter job recovered via checkpoint
(217 completed / 0 failed / 8 restored). Coordinator 0 ERROR.
Rule adopted: EVERY test in test_stage_a2_baseline_wave38.py runs with
stub curl/sleep/docker — old-code runs stay hermetic (failing-first now
0.38s, TM untouched).

## Verification
- `bash -n` clean; 4/4 new tests pass (0.24s); 4/4 fail vs old code.

```text
change_record_id: CHG-205
scope: gate-behavior
owner: saurabh
date: 2026-09-17
affected_artifacts: code/01_platform/04_scripts/stage-a2-baseline.sh, code/01_platform/04_scripts/pipeline-lib.sh, code/01_platform/04_scripts/tests/test_stage_a2_baseline_wave38.py
compatibility_class: COMPATIBLE
savepoint_impact: none — soak/baseline tooling only; no Flink job state, savepoint, or schema touched
test_updates: +4 cases in test_stage_a2_baseline_wave38.py (failing-first vs the parent commit, hermetic with stub curl/sleep/docker)
rollback_behavior: revert the commit. PHASE_OUT collides again on re-run, the uptime gate skips on a parse error, and bring-up returns to the broken scalar $COMPOSE
plan_tasks: none — wave-38 scope doc was never committed; findings P6-552/553/554 from the p6 audit §10
```
