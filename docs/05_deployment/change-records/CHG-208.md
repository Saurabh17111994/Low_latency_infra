# CHG-208 — Wave 38 commit 4: capture REST scrape guards (P6-214/215/216)

## Changes (`stage-capture.sh` sample_tick + mid-loop guard)
- P6-214: scrape split — curl to tmp file first (curl failure = dead
  endpoint), then grep; alive-but-empty warns and keeps the raw scrape.
- P6-215: scrape-dead path `return 2`, not `exit 2` — the caller's
  `sample_tick || warn (continuing)` can now resume the loop.
- P6-216: mid-loop guard debounces (sleep 5 + 2 re-polls, 3 consecutive to
  fail); the final failure still fires `capture_stall_diagnostics` loudly.
- `tests/stubs/wave38_curl.py`: scrape-empty / scrape-dead modes,
  W38_CURL_QUEUE per-poll job states, and `-o` file emulation (real curl -o
  writes the file silently — the 214 split depends on it).

## Verification
- `bash -n` clean; 4/4 new pass (~25s); 4/4 fail vs parent (~1s, fast exit 2).
- Neighbour `test_stage_capture_teardown_wave38.py` green (stub stays
  backward compatible: default modes byte-identical).
