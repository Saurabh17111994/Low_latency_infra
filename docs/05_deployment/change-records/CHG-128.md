# CHG-128 — ING-RES-001 resource contract: certified, with its scope stated

- **Status:** recorded (no code change) — evidence and residual semantics
- **Filed:** 2026-09-14
- **Source:** carry-over queue item "ING-RES-001"

## The contract

100 forced disconnect/reconnect cycles. Final FD count and goroutine count each
no greater than baseline + 2; no orphan child process/socket (`maxConn` ≤ 1); no
healthy slot interrupted (plan §1347-1363, quoted in the test header).

## Evidence

- Resource contract (unit level): `TestINGRES001OneHundredForcedDisconnectReconnectCycles`
  in `code/02_services/01_ingestion/go-bridge/resilience_100_test.go` — 100 real SDK
  connect/subscribe/drop cycles with leak assertions, while a healthy peer slot stays
  ACTIVE through the real supervisor. Backoff is suppressed there by design: "backoff
  timing is tested separately; this test targets the resource contract".
- Full wall-clock soak with real backoff: PASS 2026-08-13 — 100/100 cycles in 2852.7 s,
  goroutines 3 vs baseline 4, fds 7 vs baseline 7, `--- PASS` (hub `res001-soak`,
  `ING_RES001_REAL_BACKOFF=1`; receipt `logs/res001-real-backoff/res001-real-backoff-20260813.out`,
  as recorded by `03-ingestion.md` and `version_matrix.yaml` — the receipt itself is not present
  in this checkout: `logs/` is gitignored and the run happened on the `res001-soak` hub, so only
  the citation is durable in-tree).
- Live reconnect/replay/echo acceptance was removed by DEC-037 (2026-08-13); the bridge-level
  soak above is the reconnect evidence.

```text
change_record_id: CHG-128
scope: requirement
owner: ingestion
date: 2026-09-14
affected_artifacts: code/02_services/01_ingestion/README.md, code/02_services/01_ingestion/go-bridge/resilience_100_test.go, docs/08_implementation/03-ingestion.md, code/01_platform/04_scripts/version_matrix.yaml
compatibility_class: COMPATIBLE_WITH_LIMITATION
savepoint_impact: none — test and documentary surface only; no state, DDL or checkpoint semantics
test_updates: none — records existing evidence; no test added or changed
rollback_behavior: documentation only; revert this record. No runtime artefact to roll back
plan_tasks: none
```

## Corrected by this record

`code/02_services/01_ingestion/README.md` still described the full wall-clock soak as
"still deferred" after it had passed; that line now cites the 2026-08-13 PASS.
