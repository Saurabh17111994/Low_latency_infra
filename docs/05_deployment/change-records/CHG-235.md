# CHG-235 — the EOD controller test fails loud (wave 45)

- **Status:** recorded — fixed (2026-09-19)
- **Filed:** 2026-09-19
- **Source:** audit map wave 45 (`eod-controller-test.sh`, 17 findings, session e9f995f4).
  Eight of the 17 (P6-070…073, P6-359…362) are the audit's own `DRAFT` duplicates
  of P6-075/074/077/076/365/364/366/363 — 9 unique issues, matching the header's
  "9 HIGH".

## What was found

`feed_and_cycle` reported a failed EOD cycle with `bad()` and then fell through to
`return 0`, so its `|| rc_total=1` at both call sites never fired; the final
`exit "$rc_total"` ignored the `fail` counter entirely — a run could report
failures and exit 0. `EOD_TEST_PHASE=main` matched neither `case`, and an unknown
value ran zero phases and still exited 0 *after purging the live state table*.

Teardown was `pkill -f faketool` / `pkill -f ingestion.jar`. It appeared to work
only because container JVMs are host-visible by command line and the lib's
`docker logs -f` mirrors match the same pattern — while also matching unrelated
processes and other runs' containers. Those mirrors are started inside a
`bash -c` subshell, so their PIDs died with it and nothing recorded them.

G-EOD-2 slept a fixed 2s and then assumed the background run still held its
lease; if the mock run finished first the lease was released and the check could
pass for the wrong reason. Its `wait` had no bound. `purge_state` read
`PIPESTATUS[0]` of `( … ) | tee | grep -q`, which is the subshell's (i.e. `tail`'s,
always 0) status, so a table that was never purged still returned 0; the fixture
was a fixed `/tmp/EodPurge.java` and javac's stderr was discarded. Both status
helpers ended in `|| echo 0` / `|| true`, so a controller crash looked like "0
days" and G-EOD-0/G-EOD-1 could pass against a dead controller. There was no
EXIT/INT trap, so a signal mid-feed left containers, mirrors and fixture dirs
behind.

**P6-074/P6-071 is premise-wrong, not a bug.** It claims G-EOD-4's second mock
run should be refused with exit 5 and is flaky for not being. A completed run
releases its lease: `logs/eod-test/eod-test-20260912-002000.log` shows
`RESULT=VERIFIED EXIT=0 … DAYS=1` then `RESULT=VERIFIED EXIT=0 … DAYS=0`, and the
script's own header documents the no-op. The run pair is strictly sequential, so
no race exists. The actionable half (assert the second run's exit code instead of
inferring from `DAYS=0`) is implemented.

## What changed

- **Dispatch**: phases validated (`guards|smoke|main|all`), `main` dispatched,
  unknown values fail loudly *before* any cluster work, and the final purge only
  runs for a valid phase.
- **Exit status**: gated on `fail` as well as `rc_total`, set before the summary
  so the printed rc is the one returned.
- **`feed_and_cycle` returns non-zero** when the cycle check fails (teardown still
  runs first).
- **Teardown by identity**: the feed subshell writes the lib's own
  `FAKETOOL_LOG_PID`/`INGESTION_LOG_PID` and container names to a state file;
  `stop_feed` kills exactly those PIDs, `docker rm -f`s exactly those container
  names, cancels the Flink job by jid, then removes the state file so a later trap
  cannot signal a recycled PID. No pattern kill anywhere.
- **EXIT/INT/TERM trap** removes fixture dirs and calls `stop_feed` on any exit.
- **G-EOD-2**: bounded poll for the day record (bound 30s, ANY state — see the
  live leg below), then `SIGSTOP` on the controller JVM so the lease cannot be
  released under the check, then classify: rc=5 passes; a non-refusal *while the
  holder was still running* fails; a missed window is retried (up to 3 attempts,
  purging between them) and three misses are reported as unproven rather than as
  a violation. `SIGCONT` plus a bounded reap close each attempt.
- **`purge_state`**: `$?` under the already-active `pipefail` instead of
  `PIPESTATUS[0]`; a private `mktemp -d` fixture; javac failures reported with
  their stderr.
- **Status helpers** require that a status was *produced* rather than that the
  exit code was 0 (see the live leg below), retry an unusable result, and report
  `STATUS_UNUSABLE rc=N why=...` while keeping their output contract
  (`days_on_file` still prints a bare number). G-EOD-1 has separate messages for
  "fail-closed violated" and "fail-closed unproven".
- **G-EOD-4** asserts that run's exit code directly.

Two defects in the first version of these fixes were caught by the new tests
before commit and corrected: `days_on_file` returned `DAYS=0` instead of `0`
(breaking its callers' comparison), and the cycle flag was inverted
(`cyc_ok=1` → `return 1`).

## Live leg (2026-09-19, same day)

The guard phase had never been run — it is not in the 19-step gate and needs the
live cluster, and it drops/recreates `eod_offload_state`, which the pre-run
`status` showed to be empty (`TABLES=10 DAYS=0`), so nothing was lost. The first
run scored 4/6 and failed **two checks inside this wave's own additions**:

1. **G-EOD-2** reported "no controller JVM observable" although the concurrent run
   had been refused with `rc=5`. Cause: the poll required `state=PENDING|COMMITTED`,
   and each poll is a JVM launch (1-2s), so a fast run had already reached
   VERIFIED; a slow cluster then made the single attempt miss the window entirely.
   Fixed by polling for the record in any state, retrying a missed window, and
   classifying by exit code *and* holder liveness.
2. **G-EOD-1** reported a status-probe failure with `rc=3`. Cause: exit 3 is
   **PENDING_WORK — a documented code** (`exit: 0 ok, 1 failure, 2
   extension/retryable, 3 pending work, 4 usage/approval, 5 lease held`), emitted
   by `status` exactly when unverified days exist, which is the state a
   fail-closed run creates. The helper's rule was wrong, not the controller.
   Fixed by parsing `DAYS=` regardless of the exit code.

A diagnostic gap was closed at the same time: the failure string now carries the
first error-looking output line (`why=`), because the initial version surfaced
SLF4J class-loading noise instead of the reason.

Both corrections were first driven through the hermetic module, which grew from
23 to 29 tests (a usable non-zero status, a transient retry, a persistent
failure's diagnostic, the any-state poll and its timeout, and three scenarios of
the G-EOD-2 retry/liveness logic). The re-run scored **6 passed, 0 failed, rc=0,
state table left clean** — G-EOD-1 `rc=2 verified=0`, G-EOD-2 `rc=5, attempt 1`.

```text
change_record_id: CHG-235
scope: the EOD controller machinery test (host-side, mock executor, live cluster)
owner: platform-ops
date: 2026-09-19
affected_artifacts: code/01_platform/04_scripts/eod-controller-test.sh, code/01_platform/04_scripts/tests/test_eod_controller_wave45.py
compatibility_class: COMPATIBLE_WITH_LIMITATION
savepoint_impact: none — the test drives the controller against the live dev cluster, but it changes no job, checkpoint, savepoint, schema or service definition. Its guard phase already drops and recreates eod_offload_state (that is its documented hygiene step); no live run was performed for this change
test_updates: new tests/test_eod_controller_wave45.py (29 tests, hermetic: stubbed docker/pkill/pgrep/python3/javac/java/curl on PATH plus a temp-ROOT sandbox, no network, no live Fluss, no repo writes; ~116s, dominated by the guards scenarios' real sleep budgets) proves each guard bites: an unknown phase fails without touching the table, main/smoke are dispatched, a failed cycle returns non-zero, an unpurged table returns 1 and a compile error surfaces, the fixture is a private mktemp dir, teardown uses the recorded PIDs and exact container names and never invokes a pattern kill, a status that exits 3 with a parseable DAYS= line is USABLE while an unusable one reports why=..., a transient status failure is retried, the lease poll matches a record in any state and times out within its budget, wait_bounded kills an overstaying child, and the G-EOD-2 retry/liveness classification passes on rc=5, fails on a non-refusal while the holder runs, and reports three missed windows as unproven. Verified: Ran 29 tests in 115.824s -> OK; bash -n eod-controller-test.sh OK; EOD_TEST_PHASE=bogus -> rc=1 with no purge attempted; EOD_TEST_PHASE=guards live -> 6 passed, 0 failed, rc=0, state table left clean.

rollback_behavior: revert the commit; the test returns to exiting 0 on recorded failures, to `pkill -f` teardown, to the fixed /tmp fixture and to a fixed `sleep 2` lease check. No runtime artifact depends on this file
plan_tasks: none — audit map wave 45 remediation, not a plan task
```
