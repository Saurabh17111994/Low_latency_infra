# CHG-207 — Wave 38 commit 3: baseline floor math + receipts + evidence gate (P6-555/213/556/212-manifest)

## Changes
- P6-555 (`stage-a2-baseline.sh` top): RATE_HZ / DURATION_S positive-integer
  + SOURCE_RATE_FLOOR_PCT 1-100 validation, BEFORE mkdir/cluster. `fatal()`
  moved up and hardened for unset PHASE_OUT.
- P6-213 (`pipeline-lib.sh` new `pipeline_g25_floor_verdict`, G25 calls it):
  documented feed derivation (RATE_HZ*1024); verdicts HEALTHY / POISONED /
  INSUFFICIENT DATA (rc 0/1/2) — header-only captures never read as avg 0.
  No timeout wrapper: the TSV is tick-aggregated, no hang vector.
- P6-556: submit receipt (`submission-ids.txt`, frozen at submit) + tail
  evidence gate (stages.tsv / submission-ids.txt / stage-manifest.tsv
  non-empty, checkpoints.jsonl present) replacing success-by-`ls`.
  Dropped the audit's `-T`-stdout idea: `-T` is not a bash `test` primary
  (`[ ! -T 1 ]` is always-true); no short-run override flag — a 1-row
  capture cannot yield a rate, lengthen DURATION_S (documented in code).
- P6-212 manifest half (`pipeline-lib.sh` new `gate_manifest_add` + baseline
  call site): run_dir/rate/duration/job/backend/floor/git-sha + extras.

## Verification
- `bash -n` both; 15/15 pass (0.45s); 6 new fail vs parent commit.
- Hermetic: knob tests carry stubs (pre-fix tree can only reach still-down
  fatal); TM uptime unchanged across the failing-first run.

```text
change_record_id: CHG-207
scope: gate-behavior
owner: saurabh
date: 2026-09-17
affected_artifacts: code/01_platform/04_scripts/stage-a2-baseline.sh, code/01_platform/04_scripts/pipeline-lib.sh, code/01_platform/04_scripts/tests/test_stage_a2_baseline_wave38.py
compatibility_class: COMPATIBLE
savepoint_impact: none — soak/baseline tooling only; no Flink job state, savepoint, or schema touched
test_updates: +6 cases (failing-first vs the parent commit); G25 floor verdicts and gate_manifest_add covered by the same file
rollback_behavior: revert the commit. Floor math accepts bad percentages, G25 reads a header-only capture as avg 0, and the tail evidence gate returns to success-by-ls
plan_tasks: none — wave-38 scope doc was never committed; findings P6-555/213/556/212 from the p6 audit §10
```
