# CHG-129 — Candle-sweep disposition: four sub-findings, re-verified 2026-09-14

- **Status:** recorded (no code change) — three resolved, one deferred with a reason
- **Filed:** 2026-09-14
- **Source:** carry-over queue item "candle-sweep" (the 2026-08-13 table-kind re-scope sweep)

## 1. Count arithmetic — resolved

The sweep's numeric-drift categories (`tables-count-stale`, `acceptance-count-stale`,
`test-count-stale`, `c6-triple-stale`, `live-count-stale`) are clean on the current tree:

```text
$ python3 code/01_platform/04_scripts/stale_table_kind_scan.py
SUMMARY: 0 LIVE-STALE | 0 UNANNOTATED | 7 SECTION-ANNOTATED | 88 LINE-ANNOTATED | 0 DOC-ANNOTATED
VERDICT: no stale table-kind, phase-status, or numeric-drift claims (exit 0)
```

The scanner's truth constants (24 tables, 152 acceptance, 489/285/419 tests) match the
doc-audit C6 line, and C6 itself now runs in the fast tier (see item 1 of the same queue).

## 2. Dangling javadoc — resolved, nothing to change

No retired candle symbol is referenced in Java any more: `FormingBar`,
`CanonicalCandlePolicy` and `CandlePreviewTableSchema` appear in 0 Java files. The single
`feature_candles_15s_preview` occurrence is a negative assertion in
`DdlBootstrapSchemaAgreementTest` ("registry must NOT contain retired tables") — correct to
keep. The one intentional reference — the EOD controller comment that documents the
`candle_closed` replacement — is adjudicated "correct to keep" by
`OLD-DESIGN-RESIDUE-AUDIT.md`.

## 3. `stale_table_kind_scan.py` — resolved, it is wired

It is not an orphan script: `make stale-tables` runs it with `--upstream` and exits 1 on
un-annotated hits, `full_audit.sh` calls it (Monday-gate step 10), and
`code/01_platform/04_scripts/tests/test_stale_table_kind_scan.py` covers it.

## 4. `TENTATIVE` leftovers — deferred, recorded here

`p2-signal-computation-candles-strategies-intents.md` P2-233 was ticked while its own note
read "4 constants kept … Kept OPEN pending producer wiring". Re-verified 2026-09-14:
`VALIDITY_REASON_TENTATIVE` no longer exists anywhere in Java, and the three remaining
constants (`ACTION_CANCEL`, `VALIDITY_REASON_CONFIRMED`, `VALIDITY_REASON_SUPERSEDED`) still
have zero Java references outside their declaration file. Wiring producers to that vocabulary
is real compute work, not a docs fix, so it stays deferred — the phase-map note now says so
instead of claiming pending wiring.

```text
change_record_id: CHG-129
scope: requirement
owner: compute
date: 2026-09-14
affected_artifacts: code/02_services/02_compute/src/main/java/com/trading/compute/signaljob/SignalCandidatesTableColumns.java, code/01_platform/04_scripts/stale_table_kind_scan.py, code/01_platform/04_scripts/docs_audit.py
compatibility_class: COMPATIBLE_WITH_LIMITATION
savepoint_impact: none — documentation and dead-vocabulary record; no state, DDL or checkpoint semantics
test_updates: none — no behaviour changed; the three constants remain unreferenced
rollback_behavior: documentation only; revert this record. No runtime artefact to roll back
plan_tasks: none
```
