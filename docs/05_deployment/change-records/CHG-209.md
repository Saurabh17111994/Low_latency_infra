# CHG-209 — Wave 38 commit 5: ingestion offset robustness (P6-558/559)

## Changes (`stage-capture.sh` sample_ingestion)
- P6-558: missing-file warned-once sentinel is now `$OUT_DIR/.ingestion-warned`,
  not the offset file (reusing it poisoned resume with an empty offset).
- P6-559: offset + fsize accept digits only (default 0); the offset advances
  ONLY when the OTLP parse succeeds (`if python3 ... <<heredoc`, single copy
  of the parser — no duplication), else `offset held` WARN and retry.
- Found while testing: the end-of-run evidence gate (`.expected-outputs`)
  correctly fails rc 2 when an enabled leg stays rowless — per-tick
  resilience and end-gate loudness compose (pinned by the hold test).

## Verification
- `bash -n` clean; 7/7 pass (~47s); 3 new fail vs parent.

```text
change_record_id: CHG-209
scope: gate-behavior
owner: saurabh
date: 2026-09-17
affected_artifacts: code/01_platform/04_scripts/stage-capture.sh, code/01_platform/04_scripts/tests/test_stage_capture_wave38.py
compatibility_class: COMPATIBLE
savepoint_impact: none — soak capture tooling only; no Flink job state, savepoint, or schema touched
test_updates: +3 cases in test_stage_capture_wave38.py (failing-first vs the parent commit): missing-file sentinel, digit-only offset/fsize, offset held on a parse failure
rollback_behavior: revert the commit. The warned-once sentinel poisons the offset file again and a non-numeric offset aborts the leg instead of defaulting to 0
plan_tasks: none — wave-38 scope doc was never committed; findings P6-558/559 from the p6 audit §10
```
