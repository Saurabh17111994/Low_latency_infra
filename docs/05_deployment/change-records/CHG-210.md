# CHG-210 — Wave 38 commit 6: bounded Fluss probes (P6-560/561)

## Changes (`stage-capture.sh` sample_probes + header knobs)
- P6-560: all three probes run under `timeout 20`; stderr goes to per-tick
  `probe-{read-lag,consumer,closed}.err` (was `2>/dev/null`), and the WARN
  names the err file — a hung Admin/KV RPC can no longer wedge the run, and
  the failure stays loud.
- P6-561: probe 1 takes `${PROBE_DB}` / `${PROBE_RAW_TABLE}` (defaults
  `default` / `raw_table_1`) like the KV probes take theirs — read-lag and
  consumer-read monitor the same tables and stay comparable.
- Tests stub `java`/`javac` on PATH (argv recorder + selective 60s hang via
  real /bin/sleep); found while testing: stub probes emit no rows, so the
  end-gate rc 2 on `read-lag.tsv(header-only)` is the expected terminus and
  itself proves the probes ran.

## Verification
- `bash -n` clean; 9/9 pass (~97s); 2 new fail vs parent (~68s, old hangs).

```text
change_record_id: CHG-210
scope: gate-behavior
owner: saurabh
date: 2026-09-17
affected_artifacts: code/01_platform/04_scripts/stage-capture.sh, code/01_platform/04_scripts/tests/test_stage_capture_wave38.py
compatibility_class: COMPATIBLE
savepoint_impact: none — soak capture tooling only; no Flink job state, savepoint, or schema touched
test_updates: +2 cases in test_stage_capture_wave38.py (failing-first vs the parent commit; the old code hangs for the 60s stub sleep): bounded probe hang + PROBE_DB/PROBE_RAW_TABLE parameterisation
rollback_behavior: revert the commit. A hung Admin/KV RPC wedges the capture loop again and the read-lag probe returns to hardcoded tables
plan_tasks: none — wave-38 scope doc was never committed; findings P6-560/561 from the p6 audit §10
```
