# Fluss 0.9.1-incubating → 1.0.0 upgrade plan

**Created:** 2026-09-22 · **Status:** in progress - 51/84 done, §0 is the source of truth, 19/19 gate certified (CHG-302) · **Owner:** Platform
**Predecessor:** `../08_implementation/12-version-compatibility-evidence.md` — this plan re-certifies
every row that document currently records against 0.9.1-incubating.

## 0. Live tracker

**Working mode:** implement → verify → tick. This section is the **single source of truth** for
status; the stage sections below are the *rationale* (why each task exists, and what was measured).
Where a stage section and this list disagree, **this list wins** — the stage prose carries four
corrections recorded in §2.8 that are not yet folded back into their original wording.

**Marker legend**

| Marker | Meaning |
|---|---|
| `[x]` | done **and verified** — the task names its evidence |
| `[~]` | in progress right now |
| `[ ]` | not started, **doable offline in this environment** |
| `[L]` | needs a **live** Fluss cluster or production access — cannot be done or faked here |
| `[?]` | needs a **human decision** before it can start |
| `[-]` | deliberately **skipped or downgraded**, with the decision recorded |

**Roll-up** — every task in this plan, by stage. Counts are generated from the markers below, so
any marker change must be mirrored here in the same commit (it drifted twice before it was checked).

| Stage | Tasks | done | wip | todo | live | decide | skip |
|---|---|---|---|---|---|---|---|
| Stage 0 — Close the fork (D1) | 6 | 3 | 0 | 0 | 0 | 0 | 3 |
| Stage 1 — Bump the pins (Tier 1) | 12 | 11 | 0 | 0 | 0 | 0 | 1 |
| Stage 2 — Rebuild the image chain (Tier 2) | 20 | 19 | 1 | 0 | 0 | 0 | 0 |
| Stage 3 — Offline verification (no cluster) | 6 | 6 | 0 | 0 | 0 | 0 | 0 |
| Stage 4 — Scratch-cluster re-certification | 4 | 4 | 0 | 0 | 0 | 0 | 0 |
| Stage 5 — The four behaviour changes | 7 | 4 | 0 | 0 | 1 | 1 | 1 |
| Stage 6 — Performance re-measurement | 7 | 5 | 1 | 0 | 1 | 0 | 0 |
| Stage 7 — FIP-27 (D2) | 4 | 0 | 0 | 4 | 0 | 0 | 0 |
| Stage 8 — Docs, evidence, change record (Tier 5) | 10 | 9 | 0 | 1 | 0 | 0 | 0 |
| Stage 9 — Production cutover (D3) | 5 | 4 | 0 | 1 | 0 | 0 | 0 |
| Workflow / delivery | 4 | 4 | 0 | 0 | 0 | 0 | 0 |
| **Total** | **85** | **69** | **2** | **6** | **2** | **1** | **5** |

**Deferred with triggers (accepted 2026-09-23).** The `DEDUP_TTL_MS` config tidy-up (`docker-compose.yml`, `rollout-savepoint.sh`, `openobserve/dashboards/dedup-state.json`, ~8 test fixtures) waits for the next change that already forces a new stack generation — the docs already say the key is deprecated, so nothing is ambiguous meanwhile. The scratch cluster is built once in Group 4 (T4.0) and serves T5.2's controlled repro and T6.4's standby-replica test; it is not justified by T5.1, which is now a production-window measurement. T6.4/standby replicas remain create-time-option work that belongs to the native-adoption stage (DEC-053).


---

#### Stage 0 — Close the fork (D1)

- [x] **T0.1** Create the 1.0 tree as a worktree — `git worktree add ../Fluss_v_1.0.0 v1.0.0`; detached HEAD `5c07f88e5`. *Evidence: tag `v1.0.0` verified as an annotated PGP-signed tag (object `da44df3b9`, tagger Jark Wu) peeling to HEAD; root pom `1.0.0`; 2,908/2,908 Java files identical to the SHA512-verified source tarball.*
- [x] **T0.2** Confirm both trees independently correct — `Fluss_v_0.9.1` still on `diag/fetch-cycle-timing` at `v0.9.1-incubating`; `Fluss_v_1.0.0` at `v1.0.0`.
- [-] **T0.3** Commit the `diag/fetch-cycle-timing` branch — **SKIPPED by decision 2026-09-22.** Archiving was chosen instead, so no commit enters a third-party Apache repo.
- [x] **T0.4** Archive the tracer — `logs/fluss-fetch-tracer-20260922/` (`fetch-cycle-timing.patch` 27,076 B, both tracers, `PROVENANCE.txt`) + tracked record `2026-09-22-fluss-fetch-tracer-archive.md`. *Evidence: patch applies cleanly to a pristine `v0.9.1-incubating` worktree reproducing `7 files changed, 251 insertions(+), 2 deletions(-)`; `git apply --check --reverse` succeeds against the live tree.*
- [-] **T0.5** Commit the archive writeup — **SKIPPED by decision**; the `logs/` archive was judged sufficient. Note `logs/` is **gitignored** (`.gitignore:37`), so the artifact half is machine-local.
- [-] **T0.6** Record the tracer's *findings* — **known void, unrecoverable.** The artifact is instrumentation, not a report; the conclusions were never written down. Superseded in practice by `client.scanner.log.read-preference`.

#### Stage 1 — Bump the pins (Tier 1)

- [x] **T1.1** `code/pom.xml` L31 → `<fluss.version>1.0.0</fluss.version>`; nothing else in Maven.
- [x] **T1.2** Collect the 6 new digests **from the published artifact** — Maven Central, each cross-checked against its sha1 (all MATCH). *Evidence: `logs/fluss-1.0-digests-20260922/`.*
- [x] **T1.3** `versions.pin` → `FLUSS_VERSION=1.0.0` + `FLUSS_CLIENT_JAR_SHA256` + `FLUSS_FLINK_CONNECTOR_JAR_SHA256`.
- [x] **T1.4** `version_matrix.yaml` — all 4 rows to `proposed_version: 1.0.0`, `compatibility_class: UNKNOWN`, RE-VERIFICATION PENDING note. Prior 0.9.1 record fields deliberately retained as history.
- [x] **T1.5** `runtime.lock` + `runtime.lock.example` → `apache/fluss:1.0.0@sha256:ff461b45…`.
- [x] **T1.6** `.env` + `.env.example` → the same reference, and deliberately **tag-form, not
  digest-pinned** (corrected 2026-09-22 in the 84-item audit: both carry `apache/fluss:1.0.0`, and each
  file's own LOCAL-ONLY banner routes production digests to `runtime.lock`).
- [x] **T1.7** Re-verify the P1-052 slf4j justification — both claims hold: 38 `org/slf4j/**` entries, full API tree, all dated `2022-02-08` (slf4j-api 1.7.36), no `impl/`, `.dat` count 0.
- [x] **T1.8** Find the new ServiceLoader provider — `fluss-client-1.0.0.jar` declares `META-INF/services/org.apache.fluss.config.provider.ConfigProvider` (new in 1.0). Fed forward to **T3.5**.
- [x] **T1.9** `make pin-check` green — **PASS (6/6)**, including `version_matrix.yaml` shape and `runtime.lock` digest discipline.
- [x] **T1.10** Repair the **pre-existing** `static-check` red (6 shellcheck warnings, 3 files) — own commit `b4d9908d`. *Evidence: root-caused by controlled experiment (a directive does not apply when `source`/`.` is one command among several on a `;`-joined line); rejected the file-level disable because the repo's own docs name shellcheck's blindness to sourced code as a weakness; `static-check: 0 failures`; the 134 guard verdicts proved identical to HEAD line for line.*
- [x] **T1.11** Repair the **pre-existing** `test_compose_form_contract` red, which the first red was masking — own commit `5d14a877`. *Evidence: 3 prose sites registered `TEXT`, the 1 real invocation registered `FLAGS` and re-pointed at the invocation so it survives renames; `Ran 5 tests … OK`.*
- [-] **T1.12** `gate-fast` as Stage 1's exit criterion — **reworded and moved to Stage 2.** Six cross-file checks compare pins against Dockerfiles, so no ordering within Stage 1 alone can satisfy it; the intermediate red is expected by construction.

#### Stage 2 — Rebuild the image chain (Tier 2)

- [x] **T2.1** `fluss-runtime/fetch-jars.sh` — `FLUSS_VERSION` + 2-entry `PINS`.
- [x] **T2.2** `flink-runtime/fetch-jars.sh` — `FLUSS_VERSION` + 5 new pins; `hadoop-mapreduce-client-core` pin unchanged. The `s3`/`hdfs` pins must equal T2.1's — that equality is the anti-drift control.
- [x] **T2.3** `fluss-runtime/Dockerfile` — `FROM apache/fluss:1.0.0@sha256:ff461b45…` + 2 `COPY`.
- [x] **T2.4** `flink-runtime/Dockerfile` — 5 `COPY` to 1.0.0. `ENABLE_BUILT_IN_PLUGINS` and the compat-jar `COPY` unchanged (both version-independent).
- [x] **T2.5** `ddl-apply/Dockerfile` — **3 sites, not 4** (correction C6): the `ARG EXPECT_FLUSS` default, the L91 comment, the L93–94 `COPY` path (source **and** destination).
- [x] **T2.6** `docker-compose.yml` — **18 → 16** jar mounts (**corrected** 2026-09-22 in the 84-item
  audit: the file carries 16, not 14 - coordinator 2, tablet 2, jobmanager 6, taskmanager 6; the two
  dropped were the shaded mounts), all to `1.0.0`. Line numbers re-derived per C3.
- [x] **T2.7** B7 compose bind-source guard in `pipeline-lib.sh` (C1 — it *is* a task site) — moved in lockstep with T2.6.
- [x] **T2.8** Drop `fluss-fs-hadoop-shaded-0.9-SNAPSHOT.jar` at all 9 sites (C2). Only **1 of the 3** tests needed inversion (C2 refinement); the other two already guarded absence via `code_lines()`.
- [x] **T2.9** Re-derive and verify the compat jar — re-derived `c19c414b…`, **byte-identical** to the pin. Bundled Hadoop confirmed **3.4.3** from the jar's own `META-INF/NOTICE`, not inferred.
- [x] **T2.10** Re-stage `fluss-plugins/iceberg/` (C4) — 6 jars staged from the verified tree; every mounted jar now matches its pin.
- [x] **T2.11** Confirm the derived image is still structurally needed — **CONFIRMED** against the official 1.0.0 distribution: 10 plugin dirs, one jar per dir, so the iceberg plugin classloader still cannot resolve `S3AFileFormat` on its own.
- [x] **T2.12** `make fluss-image` — **DONE, rc=0.** Base resolved by digest: `#5 [1/3] FROM docker.io/apache/fluss:1.0.0@sha256:ff461b45…` (log lines 12/23–25 prove the pull, layers downloaded). *Evidence: `fetch-jars: all 2 artifacts match their pins.`; image `trading-fluss-runtime:0.1.0` (`84c3949af6b3`) contains `fluss-server-1.0.0.jar`, `fluss-fs-s3-1.0.0.jar`, `fluss-fs-hdfs-1.0.0.jar` and **zero** 0.9.1/SNAPSHOT artifacts (searched the whole filesystem). Note: BuildKit references a digest-pinned `FROM` by digest, so no `apache/fluss:1.0.0` tag is created — `docker image inspect` by tag fails but by digest the content is present. Stage 4's stock-image test must `docker pull apache/fluss:1.0.0` first.*
- [x] **T2.13** `make flink-image` — **DONE, rc=0.** *Evidence: `fetch-jars: all 7 artifacts match their pins.`; image `trading-flink-runtime:0.1.0` (`b8352c37357c`) ships all five 1.0.0 jars plus `hadoop-mapreduce-compat-2.8.5.jar` — the compat `--verify` that feeds **T3.6** is therefore green.*
- [x] **T2.14** `make ddl-image` — **DONE, rc=0.** Maven ran inside Docker and resolved `fluss-client/1.0.0` (log line 308). *Evidence: `image-stale: [OK ] ddl-apply … FRESH: stamp 88e9cd05b32a… matches the sources`; `image-stale: PASS — 1 image(s) current`. The P4-158 guard C5 predicted was red is green.*
- [x] **T2.15** `make check-image-stale` — **RUN, and it revealed a scope gap the plan missed.** `check-image-stale-fast` = `rc=0` (`PASS — 1 image(s) current`). The full check reported **5 stale**: eod-controller, execution-gateway, ingestion, loadgen, nautilus — because `code/pom.xml` is a **declared source** for four of them (they shade `fluss-client`) and `_platform_sources()` names `ddl_apply.py` for eod-controller. *Proof they are not caused by uncommitted work:* a clean worktree at HEAD (`6b4734b2`) reports **6** stale — the same five plus `ddl-apply`, which `make ddl-image` then fixed. So the `code/pom.xml` bump legitimately stales the ingestion-side images too; **the plan's Stage 2 covered only the fluss/flink/ddl chain.** Rebuild launched as **T2.19** — **now RESOLVED: all 8 images FRESH, `rc=0`.**
- [~] **T2.16** Empirical half of T2.11: run a tiering write against the **stock** 1.0.0 image first. If the stock image works, delete the derived image **and its Makefile target**. *(Answered 2026-09-23 from the image filesystems, not by a write: mount-free inspection shows `apache/fluss:1.0.0`'s `plugins/iceberg/` holds only `fluss-lake-iceberg-1.0.0.jar` — its fs jars sit in `plugins/s3` and `plugins/hdfs`, each with its own classloader, so the iceberg plugin cannot resolve `s3://`. The derived image copies the **identical bytes** into `plugins/iceberg/` (`sha256 5226480e…` s3, `bea49371…` hdfs) and the dev compose bind-mounts those same host files (`.gitignore:6 *.jar`), so both are load-bearing. **Do not delete the derived image or its Makefile target.** The write half is not run on dev — every datalake-enabled table is stuck `false`/absent in ZK and Fluss 1.0.0 refuses the alter — and moves to the cutover stage with T7.2.)* *(2026-09-23: the empirical half ran — the stock 1.0.0 image with the deployed plugin mounts tiered to R2 successfully, so the derived image and its Makefile target stay load-bearing and were NOT deleted.)*
  *(2026-09-23: verdict recorded in full — **keep the derived image and its Makefile target**. Stock-alone cannot work: `plugins/iceberg/` holds only `fluss-lake-iceberg-1.0.0.jar` while the fs jars sit in `plugins/s3` and `plugins/hdfs` under separate classloaders. The empirical half ran on the **stock** image plus the fs-jar mounts (the dev arrangement; `env.before` records `FLUSS_IMAGE=apache/fluss:1.0.0`): a fresh datalake table committed Iceberg objects to R2 — `lake/fluss/tier_probe_1790142504/` data + metadata + `version-hint.text`, 8 objects / 136,222 B, 0 tablet tiering errors (`logs/soak/tiering-1.0-verify-20260923T054824Z/`). **Still not run:** the same write against the derived image itself — dev serves stock + mounts, so the derived image's first real run is the cutover's (`T7.1`); its content is this same mount arrangement baked in.)*
- [x] **T2.17** **Finding F1** — fixed a pre-existing supply-chain drift: the mounted compat jar was `c5d69f9e…` (895,768 B), **not** the pin `c19c414b…` (882,001 B). Characterised: **0 of 499** shared entries differ by CRC; the mounted jar is a superset (534 entries, keeps `META-INF/` + real mtimes) → **reproducibility drift, not behavioural**. Needs its **own commit** (T-W2).
- [x] **T2.19** Rebuild the ingestion-side images staled by the bump — **DONE, `rc=0`.** `make images` then `check-image-stale` → **`PASS — 8 image(s) current`**, zero build errors. Took two attempts: the first (`b4a2e296a`) failed `rc=2` on the Fluss 1.0 API breaks (**T2.20**), which is how the plan's "zero Java changes" premise was falsified. *This was never in the original plan: found by T2.15.*
- [x] **T2.20 — RESOLVED: the upgrade DID require Java source edits; the plan's "zero Java changes" premise is FALSE.** `make images` failed `rc=2` (task `b4a2e296a`): `mvn -q -DskipTests package -pl 02_services/01_ingestion -am` → `COMPILATION ERROR` in **test** sources. `-DskipTests` skips test *execution*, not test *compilation*, so this blocks the image build and therefore `make gate`.
  Two real 1.0.0 API breaks, confirmed against the v1.0.0 source tree (not guessed):
  | API | 0.9.1 | 1.0.0 | sites |
  |---|---|---|---|
  | `org.apache.fluss.client.Connection` | 3 methods | **`MultiTable getMultiTable()` added** (`Connection.java:70`) | 3 implementors: ingestion `TypedFlussRowConverterTest:37` (`FakeConnection`), gateway `FlussControlStateStoreFlakyLookupTest:96` (`StubConnection`), gateway `FaultFlussStubs:198` (`FaultConnection`) |
  | `org.apache.fluss.metadata.PartitionInfo` | `(long, ResolvedPartitionSpec)` | **`(long, ResolvedPartitionSpec, @Nullable String remoteDataDir, int bucketCount)`** (`PartitionInfo.java:45–49`) | 1: ingestion `TickTableViewerSubscribeTest:29` |
  **COMPLETE ENUMERATION — 6 sites in 5 files, all in `src/test/`.** The first pass was wrong twice, and both corrections came from compiling rather than grepping:
  1. The `code/pom.xml` reactor declares only **3 modules**, so a whole-reactor run covers **3 of the 5 Java projects**. `02_compute` (133 Java files, 17 importing Fluss — built around **both** `fluss-flink-2.2` and `fluss-client`) and `05_mock_arrow` are standalone projects that inherit the parent via `<relativePath>../../pom.xml</relativePath>` but are absent from `<modules>`. Compiling them found a **sixth** site.
  2. Also visible: a **fifth** site (`TableInfo` arity) that no grep predicted — grep found 4, the compiler found 5, then 6.

  | project | verdict | sites |
  |---|---|---|
  | `common` | SUCCESS | — |
  | `01_ingestion` | FAILURE | `TypedFlussRowConverterTest:37` (`getMultiTable`), `TickTableViewerSubscribeTest:29` (`PartitionInfo`) |
  | `06_execution_gateway` | FAILURE | `FaultFlussStubs:198` + `FlussControlStateStoreFlakyLookupTest:96` (`getMultiTable`), `GatewayStartupPrewarmTest:182` (`TableInfo`) |
  | `02_compute` | FAILURE | `TableContractValidatorTest:617` (`TableInfo`) |
  | `05_mock_arrow` | SUCCESS | — (0 Fluss references) |

  So three distinct fixes cover all six sites: add `MultiTable getMultiTable()` to 3 fakes; add `null, bucketCount` to 1 `PartitionInfo` call; insert `null` (the new `remoteDataDir`, which 1.0 places **before** `comment`) into 2 `TableInfo` calls. **No version skew**: `02_compute`'s effective dependencies are `fluss-client:1.0.0:provided` and `fluss-flink-2.2:1.0.0:provided`.

  **Runtime risk cleared separately, because a compiler cannot see it.** Diffing the abstract-method set of all 8 Fluss interfaces we implement or proxy across both tags: only `Connection` (+`getMultiTable()`) and `Admin` (+`getClusterHealth()`, `listKvSnapshots`, `addServerTagByRack`, `removeServerTagByRack`) changed at all — `LogScanner`, `Lookuper`, `Table`, `AppendWriter`, `UpsertWriter`, `Lookup` are **method-identical**. `Admin` matters because `TickTableViewerSubscribeTest` JDK-proxies it, but all four new methods have **0 call sites** in our code, and the proxy already covers the two the test drives. Also nil: no `fluss-test-utils` dependency in any pom, no `Class.forName`/reflection on Fluss types, `04_executor` has 0 Java files, `06_execution_bridge` is 21 Go files.

  *Also: `make images` got **eod-controller** and **execution-bridge** to FRESH before failing, so it is partially effective.*

  **FIX APPLIED (2026-09-22), approved by the user — 6 sites in 5 test files, +31/−5.** Three distinct changes: `MultiTable getMultiTable()` added to 3 `Connection` fakes (ingestion `FakeConnection`; gateway `FaultConnection`, `StubConnection` — the latter two with a new `MultiTable` import alongside their existing `Table` import); `PartitionInfo(id, spec, null, 2)` at `TickTableViewerSubscribeTest:29`; and `null, null` for `(remoteDataDir, comment)` at the two `TableInfo` sites (`GatewayStartupPrewarmTest:182`, `TableContractValidatorTest:617`).
  **VERIFIED BY COMPILATION (job `b0147a538`):** all five Java projects now `rc=0` / `BUILD SUCCESS` — reactor (`common`, `01_ingestion`, `06_execution_gateway`), `02_compute`, `05_mock_arrow`. Image rebuild follow-up in `b238a9483`.
  *Process note, recorded because it nearly shipped a defect:* the first application of the `GatewayStartupPrewarmTest` edit silently changed the test's **data** — `TablePath.of("kind_check_db", "Fills")` became `TablePath.of("check_db", "Fills").toTablePath()`, inventing an API and altering a fixture datum the task never called for. It was caught only by diffing the edited files against pre-edit backups. **Diffing against a backup is what makes a scripted multi-file edit safe** — the same discipline whose absence caused the earlier document truncation.
- [x] **T2.18** SUPERSEDED 2026-09-22: the pinned jar (`c19c414b`, 882,001 B) is already on disk — the fix rode in with Stage 1/2, never split out. Rule breach recorded, not re-litigated.

#### Stage 3 — Offline verification (no cluster)

> **CORRECTION (2026-09-22, after the first full gate run failed at step 9).** This stage is named
> "Offline verification (no cluster)" and **T3.1 was filed as offline-doable. That was wrong.**
> `run-monday-gates.sh:45` states its own prereqs: *"Prereqs: Fluss up (`docker compose up -d`),
> local `~/.m2` warm, Go 1.24+, JDK 17."* The stack-dependent steps do not **skip** without a stack —
> they **fail**, and the gate is fail-fast.
>
> Measured on the run of 2026-09-22 (`monday-gates-20260922-134436`, `rc=2`): steps **1–8 all PASS**
> (static checks incl. the SC2066 repair, compose config, python 1841 tests + 546 pytest, entrypoint,
> Go `-race`, E2E binaries, docker build smoke, image staleness). Step 9 failed on two **live-stack**
> tests, neither of them touched by this upgrade:
>
> | test | message |
> |---|---|
> | `FullStackE2ETest.fullStackFakeBrokerToFluss:148` | `ING-E2E-001 pre-flight: Fluss coordinator not reachable on 127.0.0.1:9123` |
> | `FlussThroughputProbeTest.probeClientCapacity:60` | `IllegalState Fluss not reachable at localhost:9123` |
>
> **The concrete blocker:** `apache/fluss:1.0.0` is **absent from the local image store**, and so is the
> pinned digest — BuildKit's digest-pinned `FROM` pulls into BuildKit's own content store, never the
> daemon's tag list. `docker images` shows only `apache/fluss:0.9.1-incubating`. Compose then refuses:
> *"canonical compose invocation failed: Error response from daemon: No such image: apache/fluss:1.0.0"*,
> which is also why `preflight` recorded `SKIP — prerequisite missing`.
>
> The fix is one command (`docker pull apache/fluss:1.0.0`), and it needs the operator's say-so because it
> then brings up the Fluss + Flink containers. **It also unblocks T4.0–T4.3 and all of Stage 6**, which
> are currently `[L]` for exactly this reason.

>
> **RESOLVED 2026-09-22 (operator approved).** `docker pull apache/fluss:1.0.0` → `rc=0`, and the tag
> resolved to **`apache/fluss@sha256:ff461b45438033da4fd1c2556d3f978f3603bb3632fe075c2bd57388339a58cb`**
> — i.e. **exactly the digest this plan pinned in Stage 1.** That is a useful second, independent
> confirmation of the pin: Stage 1 derived the digest from the registry API, and the tag now resolves to
> the same value, so `.env`'s bare `apache/fluss:1.0.0` and the compose digest can never disagree.
>
> **SECOND BLOCKER, found immediately after (2026-09-22): the gate also refuses a DIRTY TREE.** With the
> stack finally up, the next run (`monday-gates-20260922-140412`) got past the image problem and stopped at
> **preflight**, before step 1:
>
> ```
> OK    catalog 27/27 tables
> OK    fluss-coordinator running / fluss-tablet running / zookeeper running
> OK    stack_generation=13333f976af7b640 (head 6b4734b2, catalog 27, recreations 0)
> DRIFT working tree is dirty at 6b4734b2: M code/01_platform/01_docker/.env.example; M ... (only a
>       non-certifying --steps/--sweep run may proceed)
> PREFLIGHT: FAIL (1 drift item(s)) — do NOT start the gate
> ```
>
> **`make gate` certifies a FROZEN TREE: the changes must be committed before it can run.** This is the
> missing ordering constraint — verification cannot precede the commit here, it must follow it. It also
> removes the biggest outstanding risk in this plan (the untracked plan documents), because committing them
> is now a prerequisite rather than an afterthought.
>
> *Why it was invisible until now:* the previous run reached step 9 only because preflight **skipped** — the
> missing image masked the dirty-tree drift. A skipped early step hides a blocking red exactly as a failed
> one does.
>
> Pre-flight before `make up`: **all 10 stock images the default-profile stack needs are present
> locally** (zookeeper 3.9.2, flink 2.2.1-scala_2.12-java17, node-exporter, cadvisor, otel-collector
> 0.123.0, python 3.11-slim, minio 2025-04-22, minio/mc 2025-05-21, openobserve v0.91.5-amd64,
> apache/fluss 1.0.0) — 0 missing, so the earlier single missing image was the only gap.
> `make up` starts **17 of the compose file's 21 services**; `loadgen` and the three `execution-t3`
> services are profile-gated and stay down.
>
> *Trap recorded:* the first pass of that checklist passed the list as one unquoted shell variable, and
> **zsh does not word-split unquoted expansions** — so `docker image inspect` received all ten names as a
> single argument and reported one bogus `MISS`. Re-run under Python, 0 of 10 were actually missing.

- [x] **T3.1** `make gate` — all 19 steps, cheap failures first. *Note: a red early step hides every red behind it, so "the gate was red for one reason" is never safe until the first red is cleared — demonstrated twice in Stage 1.* *(Done 2026-09-23: two clean 19/19 runs on frozen trees — 28.1 min at `1d3c2c9a` and 26.0 min at `afa2fc4d` (`logs/soak/monday-gates-20260923-001918/`).)*
- [x] **T3.2** Tier 4 test files — all 3 green: `test_flink_runtime_image` 42 OK, `test_fluss_runtime_image` 19 OK, `test_fluss_probes` 35 OK (skipped=11); `test_compose_form_contract` 5 OK; `test_ddl_apply` 20 OK.
- [x] **T3.3** Offline Fluss suites in blast-radius order (`CompatFluss*`, savepoint, signal-state). *(Done 2026-09-23: phases 1–2 green — `CompatFluss*` 10 tests + the savepoint-restore IT 1/1; phase 3 exposed a stale integration test still asserting the retired Design-B invariant, root-caused to `c0c50ee6` and rewritten to the heap-window contract in `28becc01` (green). See CHG-303.)*
- [x] **T3.4** Done 2026-09-22: every `apply.json` carries `fluss_version` + `applied_manifest_id` (e.g. `logs/ddl-apply/ddl-apply-20260905T055202Z/`); 1.0.0 stamp arrives with the cert.
- [x] **T3.5** **DONE 2026-09-22 — confirmed on the artifact that actually runs.** Inspected the shaded `ingestion` jar extracted from the built image (`01_docker-ingestion:latest`, the image the cert's step [18/19] stamped), not just the local `target/` build: `META-INF/services/org.apache.fluss.config.provider.ConfigProvider` **is present** and its body is **byte-identical** to the unshaded upstream `fluss-client-1.0.0.jar` — so `ServicesResourceTransformer` merged it rather than dropping or colliding it, and the three providers (`EnvVarConfigProvider`, `DirectoryConfigProvider`, `FileConfigProvider`) all survive. The jar carries 17 services entries, re-counted 2026-09-22 in the 84-item audit (upstream's 8 plus log4j/jackson/annotation/blockhound from other dependencies). *Method note: an earlier attempt used `unzip -l` inside the container and returned empty for a 69 MB jar — a false negative, because **`unzip` is not installed in these images**. Python's `zipfile` is the reliable tool. `R-060` covers the mechanism (`ServicesResourceTransformer` merges, P1-052's filter excludes only `org/slf4j/**`).*
- [x] **T3.6** Confirm both `--verify` passes report all pinned artifacts matched. *(from T2.13)* — **DONE.** `fetch-jars: all 2 artifacts match their pins.` (fluss-image) and `fetch-jars: all 7 artifacts match their pins.` (flink-image), both `rc=0`.

#### Stage 4 — Scratch-cluster re-certification

- [x] **T4.0** Stand up a scratch 1.0 cluster under a separate compose project name — **prerequisite for T4.1–T4.3 and all of Stage 6.**
  > **DONE 2026-09-23.** `fluss10scratch` stood up from the canonical compose file (zookeeper, coordinator,
  > tablet, Flink JM+TM — `apache/fluss:1.0.0` / `flink:2.2.1-scala_2.12-java17`); coordinator elected leader
  > (`d6c92845`, epoch 0), tablet registered on 9124, 16 slots available. The dev stack was stopped for the
  > window and restored to 12/12 containers against the pre-stop snapshot (`make up` rc=0, catalog-guard
  > 27/27 tables); the scratch stack was torn down with `-v` — project-scoped volumes only, dev volumes
  > untouched (the scratch catalog was empty on arrival, which proves they were fresh).
  > Evidence: `logs/soak/t40-scratch-20260923-020742/`.

  > **Ops note (2026-09-23) — after `make down`, a *reused* container can come back with no network.**
  > `make down` removes the compose network. `make up` recreates it, but a container that compose
  > reuses instead of recreating keeps pointing at the deleted network and returns running with none:
  > `01_docker-zookeeper-1` was Up with `networks: [] ip: []`. Nothing could resolve `zookeeper`, so the
  > coordinator never elected a leader (`UnknownServerException: NullPointerException: Cannot invoke
  > "...CoordinatorLeader..."`), 9123 never accepted connections, and the compute launcher's Babysitter
  > submit died with `Connection refused` -> HTTP 400 -> exit 1 with "degraded readiness". The
  > coordinator logged 324 retries of `Unable to canonicalize address zookeeper/<unresolved>:2181`.
  > **Remedy (verified 2026-09-23, no recreate, no data moved):**
  > `docker network connect --alias zookeeper 01_docker_trading-net 01_docker-zookeeper-1`, then restart
  > the Fluss/Flink services so their JVM DNS caches clear. After it: both servers resolved `zookeeper`
  > -> 172.30.0.11, `/dev/tcp/fluss-coordinator/9123` OPEN from the job manager, the coordinator
  > re-issued LeaderAndIsr (leaderEpoch 55), and the launcher exited 0 with
  > `Babysitter MVP no-op job is RUNNING` plus 1 completed checkpoint.
  > **Two verification lessons.** (1) `catalog-guard`'s 27/27 reads a ZooKeeper path, so it proves the
  > metadata exists - not that Fluss serves; a restore check must also assert container-to-network
  > attachment and `fluss-coordinator:9123` reachability from the job manager. (2) `make down` followed
  > by `make up` is not by itself a safe round trip for this stack.
  > **Caveat (2026-09-23, after the next `make up`):** an empty `networks` list is not by itself proof
  > of the footgun. A service declared `network_mode: "service:<other>"` borrows its peer's namespace —
  > `alert-consumer` does exactly that against `openobserve` — so it legitimately reports `net=[]` and
  > `docker network connect` refuses it ("sharing network namespace ... cannot be connected to any
  > other network"). Read `HostConfig.NetworkMode` before concluding, and verify the peer service is
  > attached. The zookeeper finding above stands: it had a normal NetworkMode and genuinely had no network.
- [x] **T4.1** Re-record the 5 evidence rows (`VM-FLUSS-SRV-005`, `VM-FLUSS-CLI-006`, `VM-FLUSS-CONN-007`, `VM-FLINK-SRV-003`, `VM-IMAGES-012`) with a **fresh date** — not edited in place as if the 0.9.1 evidence were current.
  > **DONE 2026-09-23 — six rows, not five.** The five planned rows plus `VM-JAVA-001` (it still carried the
  > last 0.9.1 client/JVM claim) were appended as fresh-dated blocks: `version_matrix.yaml` 15 → 21
  > boundaries, the 2026-08-24 rows kept verbatim as history. Classes promoted out of UNKNOWN:
  > SRV-005 → COMPATIBLE_WITH_LIMITATION, CLI-006 → COMPATIBLE, CONN-007 → COMPATIBLE; IMAGES-012
  > deliberately stayed UNKNOWN — only 3 of its 4 images were re-verified and the SBOM was not re-checked, so
  > it was not inflated. `version_matrix_verify.py` → `OK: 21 boundaries; pin discipline satisfied`;
  > `make docs-audit` all checks pass.
  > The stale pin digests quoted in `12-version-compatibility-evidence.md` rows 32/33/39 got a dated note,
  > including a same-day self-correction: `FLINK_STREAMING bb41cde` is still the live pin (Flink stayed
  > 2.2.1) while `FLUSS_CLIENT 6921994a` / `FLUSS_FLINK 5dddeb` are the 0.9.1-era digests.
  > **Gap recorded, not waived:** the nine-step `--apply-verified` contract was NOT re-issued —
  > `ddl_apply.py:756-761` requires an existing capability artifact whose only producer is
  > `ddl_apply_smoke.py` (default: the COMPAT-FLUSS-005 evidence), and that needs the dev stack. In its place
  > `make ddl` validate-only passed (27 tables, `schema_manifest.json` unchanged).
  > **Gap closed 2026-09-23:** `ddl_apply_smoke.py` PASS against the dev cluster — S1/S2 exit 0, S4
  > exit 1 via the in-band ownership gate, with the real capability artifact auto-selected (not a
  > placeholder); catalog back to 27/27 with no leftover prefixed tables. Evidence:
  > `logs/soak/t40-scratch-20260923-020742/t41g-ddl-smoke-rerun.log`. Recorded as a fresh
  > `VM-FLUSS-CLI-006` row (matrix now 22 boundaries) superseding the earlier 2026-09-23 record.
  > Self-correction, same day: the lines above say `ddl_apply_smoke.py` is the artifact's only producer —
  > that is wrong. The smoke *consumes* a capability artifact; it is the COMPAT-FLUSS-005 record
  > `logs/schema-compat/composite-pk-raw-client-20260815.md`, and the smoke falls back to a generated
  > placeholder only when that file is absent. This run let the smoke auto-select the real one.
  > **Open follow-up (2026-09-23, operator-owned):** host-side applies cannot write the default
  > evidence dir `logs/ddl-apply` — it is `drwxrwsr-x`, owned by uid/gid 10001 from the containerized
  > drill's runs — so `ddl_apply_smoke.py` falls back to a temp dir and only its log survives (the
  > per-apply JSON does not). The operator will apply ACLs at their convenience: `setfacl -R -m
  > u:<user>:rwX logs/ddl-apply` plus a default `setfacl -R -d -m u:<user>:rwX` so new evidence dirs
  > inherit. Until then, expect the temp-dir fallback and read the smoke log for verdicts.
- [x] **T4.2** Live-gated suites on a **scratch table, not `raw_table_1`**. *Evidence: the scratch-window record below — 27-table corpus applied into the scratch cluster, 5/5 ingestion live suites + the 15-class drill chain green on 1.0.0.*
  > **DEFERRED 2026-09-23 by operator decision** — no long runs until the 1.0 upgrade is verified end to end.
  > The scratch cluster's empty catalog was the intended fixture; nothing was run against it in this window.
  > **RE-SCOPED 2026-09-23 (operator: "do native from ingestion to last") — the suites are NOT excluded; the
  > empty catalog was a missing *fixture*, not a reason to skip the writer half.** Mechanism verified in source:
  > `INGESTION_INT_TEST_FLUSS=true` gates them (`FlussAppendAckTest:32`, `FullStackE2ETest:142`), bootstrap comes
  > from env (`FlussAppendAckTest:36`), and `DdlBootstrap.verifyTables` fails fast with "apply DDL via the offline
  > `make ddl`" (`:41-43`) — so the fix is a provisioning step. `RAW_TABLE_NAME` is first-class config
  > (`IngestionConfig:205`); the tests' hardcoded `raw_table_1` is harmless here because the scratch cluster has
  > its **own** `raw_table_1` (separate ZK/coordinator), which is what "not `raw_table_1`" means.
  > Measured cost from the recorded surefire XML (exclusion bought nothing): `FullStackE2ETest` 15.2 s,
  > `FlussAppendAckTest` 4.2 s, `FlussThroughputProbeTest` 2.2 s.
  > **Window composition revised accordingly:** bring up the **whole** compose under `COMPOSE_PROJECT_NAME=fluss10scratch`
  > (so ingestion/compute write to the scratch cluster by construction, not just zk/coordinator/tablet/JM/TM as in
  > T4.0), apply the corpus DDL **into the scratch cluster**, then run the live suites plus the real ingestion
  > service, then T5.2 and T6.4-standby-on in the same window. One item asserted rather than assumed, per
  > `EnableTiering.java`'s javadoc: the corpus DDL sets `table.datalake.enabled=true`, which a cluster with no
  > datalake format rejects — so phase 2 opens by checking the scratch coordinator's datalake config.

  > **Scratch window (2026-09-23) — T4.2, T5.2 and T6.4-standby-on, all three on 1.0.0.** Run under
  > `COMPOSE_PROJECT_NAME=fluss10scratch` (whole compose: own zookeeper/coordinator/tablet/JM/TM), then torn down
  > project-scoped. Measured facts: `logs/soak/scratch-window-facts.md`; evidence `logs/soak/scratch-window-*`.
  > - **Corpus DDL applied *into* the scratch cluster: 27 tables**, verified three ways — containerized apply
  >   contract (`RESULT=PASS EXIT=0 TABLES=27`), ZK count 27 under `/fluss/metadata/databases/default/tables`, and
  >   the per-table smoke — manifest `d4db8d1e0adda16a65b7f74e406c5108e10b3783908dfb9d364ba50f32137c2d`, into an
  >   isolated warehouse `s3://tradingticks-aug-2026/lake-scratch-20260923T090847Z`. The apply path that works is
  >   the **containerized** one (`docker compose run --rm --no-deps ddl-apply apply`); host-side `make ddl APPLY=1`
  >   cannot complete on a multi-tablet cluster because the advertised tablet address is compose-DNS
  >   (`UnknownHostException: fluss-tablet-2`), and `logs/ddl-apply` is uid 10001 (host-side writes fail on the ACL).
  >   Both are findings, not one-offs — the Makefile documents the container path only in a comment.
  > - **Ingestion live suites: 5/5 green on 1.0.0** — `NoBatchingTest` 4.8 s, `FullStackE2ETest` **15.18 s** against
  >   the dev baseline's 15.172 s, `FlussThroughputProbe` 2.2 s, `FlussAppendAck` 4.2 s, `IngestionNoSilentDrop`
  >   0.04 s. **`-am` is mandatory here**: without it a stale `~/.m2` `common` jar turned the same test into a
  >   90 s "failure" (`ConfigGuard: declared-but-never-read keys: [APPEND_TIMEOUT]`).
  > - **Drill chain (`make drill-live`): 15 downstream classes green**, BUILD SUCCESS. Window tally: **40 tests,
  >   2 skipped, 0 failures, 20 classes**, with the plain surefire XMLs unchanged throughout (82 / 115 / 38 / 105) —
  >   live runs are isolated to `target/surefire-reports-drills/` via `-Pdrill-reports`.
  > - **T5.2 PASS (auto-partition).** With `num-retention=3` / `num-precreate=2`: the CREATE alone pre-created today
  >   and tomorrow; a **late write inside retention** hit `NONODE` first and then **created its own partition**,
  >   storing 3 rows — A3's adoption claim; a write **outside retention** was rejected loudly:
  >   `InvalidPartitionException: Partition value '20260913' is out-of-date. The earliest retained partition is
  >   '20260920'.` **Adoption trap:** partition values must be formatted the way the table formats them
  >   (`time-unit=DAY` defaults to `yyyyMMdd`), so writers emitting `event_day` must match that or the table must
  >   carry an explicit `time-format`; the harness now cross-checks its assumed value against the server's own
  >   partitions. Note the batch sql-client exits **0** when this rejection happens — the failure is only visible at
  >   the job level, so scripted write checks must assert there.
  > - **T6.4 standby-on PASS.** With `table.replication.factor=2`, ZK showed `leader=0 standby=1` *before* the kill;
  >   killing the leader container left the data readable again in **42 s** with **zero row loss** (200 rows, digest
  >   `20019900` unchanged). Standby-off recovery took 59 s. **Precondition corrected:** the standby is drawn from
  >   the table's *assignment*, so the **table** needs `replication.factor >= 2` — not merely a 2-tablet cluster —
  >   **and** `table.kv.standby-replica.enabled=true`: at rf=2 without it the ISR holds both servers (`isr=[0, 1]`)
  >   while `standby_replicas` stays `[]`, which this window measured on a second scratch cluster (`leader=1
  >   standby=[0]` once the option was added). So the cost argument is not the storage one it was recorded as: at
  >   production's `kv.snapshot.interval=0s` the standby keeps the replicated **log with no KV store** (~1% disk,
  >   8.1M → 8.2M per tablet) and the write path showed no detectable penalty (7.1 s vs 7.3 s over 10 rounds each).
  >   The real dial is `kv.snapshot.interval`: `0s` = near-free standby with replay-based promotion (unmeasured),
  >   `30s` = the 42 s promotion above at snapshot cost.
  > - **R2:** no cleanup needed — zero objects under either scratch prefix. For the record, the tiering
  >   verification's prefix now holds 6 objects / 136124 bytes rather than the 8 / 136222 captured that morning
  >   (Iceberg metadata cleanup is the likely cause; not measured). **Basis re-tested 2026-09-23:** `r2_list_all`
  >   returns the whole bucket — **3965 objects in three identical runs**, rc=0, clean stderr — which is exactly
  >   `r2_list_lake`'s 3769 plus `remote-data`'s 195 plus `flink-checkpoints`' 1, and **none** of them are under
  >   `lake-scratch-*`. An earlier count of 1000 was a *partial listing after an R2 timeout*, not truncation — the
  >   tool paginates correctly, and counting stdout without reading stderr is what made it look like a bug. The same
  >   runs put the tiered-log prefix `remote-data/` at 195 objects — **and that prefix is a caveat to the claim
  >   above:** `remote.data.dir` is hardcoded to `s3://<bucket>/remote-data`, so it is *not* isolatable by the
  >   scratch warehouse override, and those 195 objects cannot be attributed to this window either way. "Zero
  >   objects under either scratch prefix" is exact, but it only ever covered the *warehouse* prefixes.
  > - **Dev stack restored:** 13 volumes intact before and after, catalog-guard 27/27, JM → `fluss-coordinator:9123`
  >   OPEN, host → 9123 OPEN, Flink job RUNNING. Scratch project gone with its 10 volumes.
  > - Further findings in the facts sheet: bare `SHOW TABLES` is a **vacuous check** on this catalog (the corpus is
  >   in `default`, which needs backticks and is not the catalog's own `fluss`), and the second tablet has no host
  >   port by construction — so host-side suites are necessarily single-tablet.
- [x] **T4.3** Confirm the ACL and AWS-credential traps are genuinely non-events, and record *how* that was established.
  > **DONE 2026-09-23 — both traps re-confirmed, with the method; one premise corrected.**
  > **ACL:** zero `createAcls`/`dropAcls`/`sys.add_acl`/`sys.drop_acl` calls anywhere in `code/` or `docs/`
  > (the single grep hit is this plan quoting the strings), against the v1.0 rule that ACL *modification* now
  > requires `ALL` (`upgrade-notes-1.0.md:10-14`). No principal manages ACLs here.
  > **AWS credentials:** the 2026-09-22 sweep's premise was incomplete — a provider *is* set:
  > `SignalJob.java:556` → `org.apache.hadoop.fs.s3a.SimpleAWSCredentialsProvider`. That class is supplied by
  > `fluss-fs-s3-1.0.0.jar` (on the Flink classpath via the compose mount), which holds **0** `com/amazonaws/`
  > (SDK v1) entries and **6,656** `software/amazon/awssdk/` (v2) entries — so the notes' trigger ("explicitly
  > references an AWS SDK v1 provider", `:18-22`) does not apply, and static `fs.s3a.access.key`/`secret.key`
  > are separately exempted (`:34`). Live corroboration from the scratch cluster: the tablet logged
  > `S3FileSystemPlugin — Using provided static credentials` on Fluss 1.0.0.

#### Window #2 (2026-09-23) — replication cost measured, read-preference deferred with reasons

A second scratch project (`COMPOSE_PROJECT_NAME=fluss10scratch`, two tablets, warehouse
`s3://tradingticks-aug-2026/lake-scratch-20260923T094956Z`, `kv.snapshot.interval: 0s` = the production default)
run under the approved scope "A4 cost + T6.3". Evidence: `logs/soak/w2-20260923T094956Z/`.

- **A4 — the cost claim was an overstatement at the default, and the precondition is two options.** Table
  `a4_rf2_*` differs from `a4_rf1_*` only by `table.replication.factor` (4 buckets, 5 rounds × 1000 rows each,
  round-robin, duration read from the JobManager's terminal-state line). **Placement:** rf=1 → `standby=[]`,
  `isr=[<leader>]`; rf=2 **alone** → `isr=[0, 1]` with `standby_replicas` **still `[]`**; adding
  `table.kv.standby-replica.enabled=true` → `leader=1 standby=[0]` per bucket. **Cost at `0s`:** per-tablet
  footprint `8.1M` (rf=1) vs `8.2M` (rf=2), with `kv-` directories only on the buckets each tablet *leads* and
  `log-0..3` on all — i.e. the standby carries the replicated **log and no KV store**, ~1%. **Write path:** mean
  7.1 s (rf=1) vs 7.3 s (rf=2) over 10 rounds; ±1 s timestamp resolution, so no penalty is detectable at this
  granularity. The dial is therefore `kv.snapshot.interval`: `0s` = near-free standby with replay-based
  promotion (unmeasured), `30s` = the 42 s promotion window #1 measured, at snapshot cost.
- **T6.3 — deferred to production, with four measured reasons** (see the T6.3 entry). Both the local attempt and
  its failure modes are recorded there; the `[L]` marker stands.
- **Findings worth carrying (all measured this window).** (i) `FLUSS_REMOTE_LOG_TASK_INTERVAL=0s` disables
  remote-log tiering (`RemoteLogManager.remoteDisabled`); the **compose default is `1m`**, so production has
  tiering **on** while dev has it off deliberately. (ii) Tiering copies **rolled** segments, so a table needs
  either enough incompressible volume or a small `log.segment.file-size` before anything can tier. (iii) The
  batch sql-client planner OOMs (`java.lang.OutOfMemoryError`) on a ~1 MB `VALUES` statement — chunk bulk
  inserts. (iv) A hand-rolled `docker compose` invocation **must** pass both
  `--env-file .../.env --env-file .../secrets.env`, exactly as the Makefile's `COMPOSE` and window #1's runner
  do; without them the AWS credential defaults silently become `minioadmin`, which surfaces as an S3
  `AWSBadRequestException` rather than as a missing flag.
- **R2, corrected after the fact:** the window did leave objects behind — **4 under its own warehouse prefix**,
  two per `t63_*` table (`metadata/v1.metadata.json` + `metadata/version-hint.text`), written by the two
  datalake-enabled CREATEs that succeeded before the tiering checks failed. `remote-data/` is unchanged at 195
  (tiering never ran). This bullet first claimed "no new objects"; the next complete listing — 3969 total, against
  the 3965 reconciled in the R2 entry above — is what caught it, before the claim was committed.
- **A promoted script is not a verified script.** Moving the restore-and-verify runner from `logs/soak/` (where
  `../..` is the repo root) into `code/01_platform/04_scripts/` (three levels deep) silently broke its `cd`: the
  restore then ran `make up` inside `code/`, got `No rule to make target 'up'`, and **reported success anyway**,
  because it had no failure gate — leaving the dev stack down while printing `DONE`. Both defects are fixed (the
  depth, and a verdict that exits non-zero unless `make up` succeeds, containers exist, `catalog-guard` reports
  27/27 and the JM reaches `fluss-coordinator:9123`).
- **Teardown and restore, as it actually happened.** The scratch project is gone with its own volumes (17
  removals; zero scratch containers or volumes remain; dev volumes 13 before and after). The **first restore
  attempt failed silently** and left the dev stack down — the promotion lesson above — and the corrected script
  then brought it back and verified it: `make up rc=0`, 13 containers on `01_docker_trading-net` (alert-consumer
  `net=[]` by design), coordinator resolving zookeeper `172.19.0.3`, JM → `fluss-coordinator:9123` OPEN, host →
  9123 OPEN, `catalog-guard` 27/27, Flink 2.2.1 with 1 TM / 15 slots, job RUNNING. Evidence for the restore:
  `logs/soak/stack-restore-20260923T102441Z/`. The teardown run's own R2 section reported prefix `unknown/` (a
  stale variable, now fixed) and is superseded by the complete 3969-object listing counted above.

#### Window #3 (2026-09-23) — standby promotion measured, A4's cheap mechanism closed, host-reboot hazard found

Scope: the A4/A5 adoption probes and the promotion measurement the A4 decision was waiting on. No Stages 5-8
work happened in this window.

- **A4's benefit is real, and it holds at the production default.** Drill `logs/soak/t64c-promotion-drill.sh`,
  evidence `logs/soak/t64c-promotion-20260923T104425Z/`. Fixture `t64c_promo_1790160265` (tableId 14409) with
  `replication.factor=2` + `table.kv.standby-replica.enabled=true`, `kv.snapshot.interval=0s` asserted in the
  rendered config; bucket 0 held `leader=1 standby=[0]` before the kill. The leader
  (`01_docker-fluss-tablet-2-1`) was killed and **left `exited` for the whole measurement** — window #1's 42 s
  cannot be attributed to the standby, because that drill restarted the killed leader mid-run
  (`kill; sleep 15; start; then poll`). Result: **leadership moved in ZK at +36 s, data was readable again at
  +56 s, zero row loss** (digest `20020100` either side, 200 rows). Footprints confirm the standby carried the
  replicated log only at `0s`: tablet-1 1.5G with `kv-dirs=1 log-dirs=1`, tablet-2 184K with
  `kv-dirs=0 log-dirs=1`.
- **The designation is single-use.** At 20 000 rows (digest `20000200010000`) the second kill was **refused by
  the drill's own guard**: `bucket0 before kill #2: leader=0 standby=[]`. ZK moved from
  `{"leader":1,"leader_epoch":0,"isr":[1,0],"standby_replicas":[0]}` to
  `{"leader":0,"leader_epoch":1,"isr":[0,1],"standby_replicas":[]}` — promotion consumed the standby and did
  not restore it within the run, so this is **one accelerated promotion per bucket per designation**, not a
  permanent second copy. The scaled (20 000-row) promotion therefore stays unmeasured.
- **A4 cannot be delivered in place.** Two probes, both self-cleaning (volumes 13 to 13, catalog-guard 27/27):
  a single-tablet CREATE with `replication.factor=2` is rejected — `InvalidReplicationFactorException:
  Replication factor: 2 larger than available tablet servers: 1.` (`logs/soak/a4-dev-probe-20260923T104811Z/`)
  — so the shared DDL corpus cannot carry the option; and on a two-tablet dev cluster
  `ALTER TABLE ... SET ('table.replication.factor'='2')` is rejected by name (`InvalidAlterTableException: The
  following options are not supported to alter yet: 'table.replication.factor'`) while
  `table.kv.standby-replica.enabled` is accepted and **inert at rf=1** (ZK stayed `standby_replicas=[]`,
  `isr=[0]` — `logs/soak/a4-alter-probe-20260923T104939Z/`). A4 is therefore **reclassified as a DR follow-on (`DEC-055`)**, blocked on an audit-to-KV
    reconstruction path that does not exist (measured 2026-09-23: every mechanism runs KV to job, or lake
    to parquet). The mechanism is kept as the design for whoever builds that path; the numbers live in
  `2026-09-22-fluss-1.0-native-adoption.md`.
- **A5 landed** (`f9b0e9e4`): the production tablets now read `kv.snapshot.interval` from
  `${FLUSS_KV_SNAPSHOT_INTERVAL:-0s}`. The default is unchanged, so nothing moves until an operator sets it.
- **Doc hygiene, deliberately bounded** (`84588763`): the six live documents that still described 0.9.1 as
  current now state 1.0 only, and `02-environments.md` gained a "Stack versions" section. Dated history was
  **kept on purpose** — the append-only matrix, the `CHG-*` records, the plans, the compatibility-evidence
  document, the gate logs, and the code comments that explain why a guard exists all retain their 0.9.1
  references, because the before/after is what shows the upgrade was validated. Roughly 320 references remain
  repo-wide; **zero** remain in live config or docs.
- **Host reboot (2026-09-23 16:05 local) — a hazard, not a Fluss fault.** `dockerd` started at 16:05:49
  *before* the ecryptfs mount behind `/home/saurabh` (`.Private`) was ready, so the eight containers that
  bind-mount jar files failed to start: `error mounting ".../fluss-fs-s3-1.0.0.jar" ... not a directory: Are
  you trying to mount a directory onto a file (or vice-versa)?`. Services without file binds came up normally,
  so the stack looked half-alive. A failed mount is not retried by any restart policy, so the remedy is the
  gated restore runner: `code/01_platform/04_scripts/stack-restore-verify.sh` → `make up` rc=0, 13 containers,
  `catalog-guard` 27/27, the compute job resubmitted and RUNNING, `RESTORE VERDICT: OK` (evidence
  `logs/soak/stack-restore-20260923T104246Z/`, 16:12 local). **Production is immune**: the Swarm stack mounts
  no Fluss jars at all (`T9.4`), which is why this can only bite the dev Compose stack. One residue worth
  fixing later: the runner writes its sub-logs into the evidence directory but the `RESTORE VERDICT` line
  reaches stdout only, so the verdict is not self-contained in the evidence.

#### Stage 5 — The four behaviour changes

- [L] **T5.1** **B1 (sharpest risk)** — measure how often a late tick lands in an `event_day` partition older than the current day. *(Originally recorded as answerable offline from `raw_table_1`; superseded by the dated reclassification below — a local count would only measure harness injections.)* It decides whether B1 is a scheduling note or a blocking defect. *(Reclassified 2026-09-23 to the cutover window — no local measurement is possible: this stack runs no ingestion feed (only `fluss-tablet`/`fluss-coordinator` are up, and the gate's own preflight says no step uses the ingestion service), so a local count would only measure harness injections. Measure on production for the first week after cutover: `compute.candles.late.dropped` (provisioned, "should stay flat") plus the visible partition write-failure log. The behaviour itself is recorded as by-design in the DDL, ticket P4-170: `event_day` is format-checked but not recency-checked, an old `event_time` writes to its old partition, and a write outside the 9-day retention fails visibly — never silently.)*
- [x] **T5.2** **B1** — reproduce the seek: *(PASS in the scratch window below: a late write inside retention creates its own partition and lands; outside retention is rejected with `InvalidPartitionException` naming the earliest retained partition.)* —  write to an old `event_day` partition while a 1.0 source runs. *(This is now the local half of T5.1's question: a controlled repro on the Group-4 scratch cluster, because the dev stack has no production traffic to measure.)*
- [?] **T5.3** **B1** — decide and record the mitigation: accept it (and bound the duplicate window) or guard the source with explicit per-partition offset tracking. *(Provisional disposition 2026-09-23: accept B1 and bound the duplicate window to the dedup horizon — 200 entries per token ≈ 10 s, DEC-054 — conditional on T5.1's cutover-window measurement. Escalate to per-partition offset tracking only if production shows late ticks reaching older `event_day` partitions often enough to exceed that horizon.)*
- [x] **T5.4** **B2** — sweep for consumers of the removed `fluss.reader.bucket.<n>.currentOffset` gauges. *Evidence: exact-string sweep — `current_offset` and `flush.reader.bucket` **no hits at all**; every hit for `currentOffset`/`fluss.reader.bucket`/`reader.bucket` is this plan document; `fluss-client-metrics.py` contains no bucket/offset/gauge reference. **Zero consumers** → no dashboard or alert can be silently wrong.*
- [x] **T5.5** **B3** — bucket-count sensitivity. *Evidence: all **28** DDL tables request 4–16 buckets (max 16); 1.0's own config description declares `max.bucket.num` a **per-table/per-partition cap at 4096**, added to keep the assignment znode under ZooKeeper's packet limit. **Inert for us.***
- [-] **T5.6** **B4** — downgraded to **no action**: `remote.data.dir` is still present in 1.0 (`ConfigOptions.java:113–122`, `noDefaultValue()`, not deprecated); `remote.data.dirs` is additive. We set the singular key in 7 places and `dirs` is unset everywhere → no R2 data becomes unreachable.
- [x] **T5.7** Keep a real R2 read — now a **general migration check** ("the upgraded server still reads what the old one wrote"), not a B4-specific hunt. *Done 2026-09-23, read-only, evidence `logs/soak/t57-r2-read-20260923-013432/`: all five tables under `lake/default/` read via `r2-query.sh` (DuckDB iceberg_scan), rc=0 on every query — `raw_table_1` **6,945,080 rows** (entirely `event_day=20260831`, which closes T7.4's stale-day note), `candle_scale_log` **2,529,054 rows**, and `feature_candles_15s`/`Signal_Candidates`/`Signal_Candidates_current` 0 rows (metadata-only tables, still parsed cleanly). Sample rows keep every column, including the Fluss bookkeeping columns (`__bucket`, `__offset`, `__timestamp` 2026-08-12T13:53:41+05:30). Scope: this proves the **lake data format** written by 0.9.1 is migration-safe under the upgraded toolchain; the Fluss server's own lake-read path is T7.1's cutover check, not this one. Slowest query 237s (3,439 small objects) — pure metadata enumeration, which is why the smoke filters by `event_day`.*

#### Stage 6 — Performance re-measurement

- [x] **T6.0** **Locate the 42-row perf baseline.** Not found under `logs/` by name; `logs/tracker-14/sig-perf-001-50k-baseline-20260817.md` is the only perf artefact identified so far. Needed *before* any comparison. *(Answered 2026-09-23: declared unrecoverable — the only `holistic-measure` dir (2026-09-19) has an empty `main/`; no file under `logs/` or `docs/` carries `A2b` as content; the 2026-08-17 50k attempt failed at 90 s warmup — 2433 tokens > capacity 1024 — with F.2 NOT RUN and F.3 NOT EVALUATED.)*
- [x] **T6.1** Writer tuning p99 + the 50k ticks/s baseline, same harness (`INGESTION_INT_TEST_PERF`). *(2026-09-23: the **writer half** is measured on the live dev stack — `logs/soak/t61-throughput-probe-20260923-005733/`: `rows=20480 rows_s=49700 avg_ms=7.686 p50_ms=7.258 p99_ms=17.131`, i.e. ~2.4× the 20,480 rows/s bench target and 3.3× the ≥15k rows/s stop/go bar. Still open: the full-harness counterpart (the aborted `T6.2`) and the **linger sweep** (`BatchLingerSweepProbe`, THR-PROBE-002), which has no evidence yet.)*
  *(2026-09-23 — **linger sweep closed (THR-PROBE-002)**, no `make holistic` needed for it:
  `logs/soak/t61-linger-sweep-20260923-175452/` (mvn_rc=0, 6 runs x 20,480 rows, `failed=0` in all six).
  linger 0 ms -> 60,267 rows/s p50 5.31 p99 12.72 (first run, includes connect) - 1 ms -> 166,553 p50 4.14
  **p99 9.44** - 5 ms -> 206,960 p50 5.68 p99 10.50 - **10 ms -> 296,565 rows/s p50 9.58 p99 16.96** -
  20 ms (the binding `client.writer.batch-timeout` default) -> 249,103 p50 20.26 p99 30.16 - 50 ms ->
  134,259 p50 41.19 p99 60.65. 10 ms dominates the current 20 ms default on **both** axes: recorded, not
  acted on (`docs/08_implementation/03-ingestion.md` L161 says do not pre-emptively tune). Marked `[x]`:
  this task's own text is "writer p99 + the 50k ticks/s baseline" (49,700 rows/s measured by the T6.1
  probe, linger resolved here); the end-to-end counterpart is T6.2's deliverable.)*
- [x] **T6.2** The **A2b-v3 disk-saturation signature** (150–200 MB/s, ~90 s bursts, 35/42 aligned across 3 runs) — confirm it still looks the same. *(Re-scoped 2026-09-23: with no historical baseline, this is a qualitative re-measure on 1.0.0 — the shape (150–200 MB/s, ~90 s bursts) — and no before/after ratio is computable.)*
  *(2026-09-23 17:44 — the re-run **aborted at the smoke inject gate**, and the cause is the harness, not
  the pipeline. The smoke phase printed `SMOKE PASS` while `smoke/metrics-final.txt` recorded
  `multi-tf-aggregator | 2,256,782 | 0` with every candle sink `0 | 0`, so `compute.candles.late.dropped`
  could not move (gate: `want dups=200 late=20; got dups=200 late=0` — the dedup half was exact).
  Root cause: the run was **outside 09:15-15:30 IST**, where `TimeframeBucket` drops every tick before any
  state is touched, and the monotonic gate that owns that counter needs a *newer* event time already in the
  slot — so an injected 90 s-late frame looks like that token's first tick and takes no lateness branch.
  **Not** a 1.0 difference: the 0.9.1-era run `logs/tracker-14/holistic-measure-20260919-010036` (01:05 IST)
  has the identical shape — aggregator out=0, sinks 0/0, late counter flat across all 8 subtasks — while the
  last green inject gate (CHG-194 run 11, **09-17 11:04 IST**) was in-session. Fixed by **CHG-304** (the
  harness decides the session state itself and fails a smoke that aggregated nothing). **Verdict, run
  `logs/tracker-14/holistic-measure-20260923-180457` (off-hours, harness deciding): the session half is
  validated.** `session.txt` records `MULTITF_SESSION_BYPASS=true ist_minutes_now=1084 session_open=false`;
  the new gate printed `multi-tf-aggregator emitted 2770328 records in 180s`; and the inject gate that read
  `late=0` at 17:44 now reads `dups=200 late=20` against `want 20`, with the smoke and main G8 fingerprint
  gates PASS. This is the **first populated `main` phase ever recorded** (all three prior runs had `main=0`):
  9,681,212 raw rows over 900 s (10,757/s, feed-bound at 10,240/s), dedup -800 = exactly the 4x200 injected
  dups, aggregator 9,680,094 in -> 11,721,769 out, `candle_closed` 123,904 rows (64,512 candles),
  `candle_live` 11,597,865; GC live-set 167MB (bound 200MB), 0 pauses >= 100ms, 0 latency bursts, F6
  rejections 0, G7a 800/800, G7b 80/80, F8 80 backward jumps = 80 late rows, 3 legs UNAVAILABLE by design.
  **T6.2's own question answers NO: the A2b-v3 disk-saturation signature did not reproduce** - `A2 VERDICT:
  disk not saturated this run` (busy% max 33, 0 intervals >80%, await max 9 ms/op) and `A2b VERDICT:
  heaviest writer = 'tablet', max 24 MB/s, spikes do not align with bursts`, against the 0.9.1-era signature
  of 150-200 MB/s in ~90 s bursts. Standing caveat: that signature came from a different workload, so this
  is a qualitative re-measure, not a like-for-like pair. **The run still exited 2: G7c candle-vs-raw parity
  is RED** - 1188 of 62,464 fully-closed (token,window) pairs mismatch, the candle side short by 1-5 ticks of
  ~150 (e.g. window 1790167335000 token 583: candle ticks 147 vol 3427 vs raw 150 vol 3481). This is the
  first time that leg ran with a *populated* candle side (CHG-194 run 11's 22,528 mismatches came from an
  empty side), so it is new information rather than a 1.0 regression - and the analyzer records the
  mechanism itself in the same output: "the late-drop counter is 6619 above the 80 injected late ticks
  ingested in-window - those are natural feed re-feeds the aggregator drops by design (monotonic gate)".
  G7c compares the raw window count without subtracting those drops, and the total shortfall (~3.5k across
  1188 windows) is the same order as the 6,699 measured in-window drops, so **loss vs accounting is NOT
  settled by that run alone - **fixed as CHG-305** (totals reconciliation against the aggregator's own drop counter) and re-measured green below.
  *(Early-signal item settled 2026-09-23: `Signal_Candidates` holding 0 rows in-window is the **expected
  state** in this topology, not a defect. The job was submitted with `STRATEGY_HOST_ENABLED=false`, and that
  host is the ONLY writer of the table — `holistic-analyze.py`'s `strategy_host_enabled()` docstring declares
  it: "with it off an empty read of that table is the expected state, not a broken probe". The 6 rows the
  reader did return are dev-token leftovers (`B4OFF-EQ`/`B4UP-EQ`, rule `n7-range-breakout-v1`) written
  17.5-25 h before the run window, and the harness never purges that table (its prologue purges
  raw/candle_closed/candle_live only) — which is exactly why the analyzer window-filters it. So the "0 rows in
  run window" line is correct, and the leg carries no signal until a run submits with the strategy host on.)*
  **Re-measured green (run `logs/tracker-14/holistic-measure-20260923-184211`, same off-hours conditions,
  with the CHG-305 reconciliation): `make holistic rc=0`.** `- G7c parity: 62464 fully-closed (token,window)
  pairs compared candle-vs-raw, 0 informational mismatch(es); tick shortfall=0 vs 80 counted drop(s)
  in-window`, then `G7c reconciliation: shortfall=0 raw tick(s) covered by 80 counted drop(s) (80 of them
  were ticks already counted, i.e. re-feeds ...)` and `G7: data-quality guards passed (dedup exact,
  late-drop covered, tick-set parity reconciled)`. The previous run's drop population did **not** reproduce:
  `G7b sent=80 raw-observed=80 counter-delta=80` (was 6,699) and the 1,188 mismatching windows are gone (0).
  Everything else held: 9,625,301 raw rows read over 946 s, dedup -800 = exactly the 4x200 injected dups,
  aggregator 9,624,318 in -> 11,654,394 out, G7c candle side 64,512 closed 15s candles = 1024 tokens x 63
  fully-closed windows, A2 `disk not saturated this run` (busy% max 55, await max 9 ms/op, 0 intervals >80%)
  and A2b `tablet max 26 MB/s, spikes do not align with bursts` - **T6.2's question answers NO a second
  time** - GC live-set 167MB of a 200MB bound, 0 pauses >= 100ms, 0 latency bursts, G6 latency guards
  passed, the same 3 legs UNAVAILABLE by design. **Marked `[x]`**: its question (does the A2b-v3
  disk-saturation signature still look the same?) is answered NO on two independent runs, and the leg that
  was red is now measured, reconciled and green. Recorded as an observation, not an open item: why the 18:04
  run carried 6,619 extra counted drops (out-of-order arrivals the monotonic gate refused) while this one
  carried none - the reconciliation prints both numbers on every run now, so a recurrence is visible at
  once.
  `main` phase exists on disk** (`main=0` in all three recorded runs), so T6.6's "beside the 0.9.1 numbers"
  comparison target does not exist and must say so.)*
- [L] **T6.3** Fetch latency, local vs remote — where `client.scanner.log.read-preference` pays off.
  *(Attempted locally 2026-09-23 and **stopped by decision** — the `[L]` now rests on measurements, not caution.
  Four attempts, four distinct causes, all fixed and all worth keeping: (1) a hand-rolled `docker compose`
  invocation without `--env-file` falls back to compose's `minioadmin` AWS defaults, so the datalake CREATE
  failed with `AWSBadRequestException` while the *test* reported success (see the window-#2 record below);
  (2) with `bucket.num=1` the table landed on **tablet-2**, which the committed T6.4 override deliberately
  points at a container-local `remote.data.dir`; (3) `FLUSS_REMOTE_LOG_TASK_INTERVAL=0s` in the dev `.env`
  **disables remote-log tiering by design** (the A2b burst fix — copying rolled segments saturated the single
  NVMe), so nothing could reach `remote-data/`; (4) `LogTieringTask` copies **rolled** segments, and 4 MB of a
  repeated payload compresses into a ~10 KB active segment that never rolls. The final attempt died on
  `java.lang.OutOfMemoryError` in the sql-client planner from a ~1 MB `VALUES` statement. **Conclusion:**
  production runs the compose default `1m`, i.e. tiering **on** — so the preference is a real lever at cutover —
  but this dev box cannot produce the comparison: tiering is off here on purpose, and the "remote" leg would be
  R2 over a dev internet link rather than production's fetch path. Measure during the cutover window.*)
- [x] **T6.4** KV recovery, first with `table.kv.standby-replica.enabled` **off**, then on. *(Both halves done: off = 59 s dev drill, on = 42 s with a ZK-proven standby and zero row loss; see the scratch window below for the `replication.factor >= 2` precondition.)* *(Dev half
 **DONE** 2026-09-23 — drill `logs/soak/t64-kv-recovery-drill.sh`, evidence
 `logs/soak/t64-kv-recovery-20260923T051901Z/`: 200 rows in a prefixed KV table, tablet killed
 abruptly and restarted, digest identical before and after (`20020100` = count+sum) — **zero row
 loss**, readable again **59 s** after container start. That is the single-replica baseline; the
 standby-**on** half still needs a >=2-tablet cluster (dev runs one) and belongs to the native-adoption
 plan section 3.)*
- [x] **T6.5** Keep `log.retention.roll-active-segment.enabled` **disabled** through the whole rolling upgrade. *(standing constraint — tick only at cutover)*
- [~] **T6.6** Record the new numbers **beside** the 0.9.1 ones, never replacing them. *Partly done 2026-09-22: the **gate-timing** pair is recorded beside its predecessor in `CHG-302` (83.4 → 28.1 min) and in the gate-state block of `00-start-here.md`. Still open for the numbers T6.2–T6.4 produce — those tasks are `live` (prod-stack dependent), so this stays unticked: the certificate measures the gate, not the disk/fetch/KV-recovery baselines.  
  *Update 2026-09-23 — the **writer** half of T6.1 is recorded here too: the dev-stack probe measured `rows=20480 rows_s=49700 avg_ms=7.686 p50_ms=7.258 p99_ms=17.131` (`logs/soak/t61-throughput-probe-20260923-005733/`), i.e. ~2.4× the 20,480 rows/s bench target and 3.3× the ≥15k rows/s stop/go bar. The 0.9.1 predecessor for this row is the 42-row baseline T6.0 could not recover, and the full-harness counterpart (T6.2) was aborted on 2026-09-23 by the "correctness before measurement" directive — its run is labelled in `logs/tracker-14/holistic-measure-20260923-005820/ABORTED.md` and produced no usable numbers.*
  *(Update 2026-09-23, after the T6.2 re-run — every pair that can exist on this host, each beside its 0.9.1
  counterpart, plus the pair that provably cannot:
  (a) **smoke inject accounting, 1.0 off-hours vs 0.9.1 in-session** — 1.0 read `dups=200/200 late=20/20`
  with `multi-tf-aggregator 2,770,328 out in 180 s`; the 0.9.1-era CHG-194 run 11 (2026-09-17 11:04 IST,
  same 10 Hz x 1024 feed) read `200/200 duplicates and 20/20 injected late frames` with G7a 400 / G7b 40.
  The 1.0 main phase injected 4 rounds (G7a 800 / G7b 80), so the **per-round** accounting is identical two
  versions apart: 100 % of injected duplicates and 100 % of injected late ticks dropped and counted.
  (b) **disk signature** — 1.0: tablet max 24 MB/s, busy% max 33, 0 intervals >80 %; 0.9.1-era A2b-v3
  signature: 150-200 MB/s in ~90 s bursts. Different workloads, so this stays qualitative (T6.2's caveat).
  (c) **writer vs feed** — 1.0 dev-stack: 49,700 rows/s writer probe (p99 17.1 ms) and 296,565 rows/s linger
  sweep at 10 ms; the end-to-end path moved 9,681,212 rows in 900 s = 10,757/s, i.e. **feed-bound**
  (10,240 ticks/s + injected dups), so the 1.0 writer has ~4.8x headroom over the feed. No 0.9.1 writer
  probe exists on this host.
  (d) **the pair that cannot exist here**: a 0.9.1 *main-phase* record. All three pre-existing holistic runs
  have `main=0` (the phase never ran), so "1.0 main phase beside 0.9.1 main phase" is unbuildable on this
  host — T6.2's verdict records that too. Production is the only place the counterfactual exists: it runs
  the 0.9.1 pin today, so **T7.1-T7.3 must capture the 0.9.1 side before T9.1 flips it**. That is why this
  marker is `[~]` and not `[x]`: the recording halves are done, the production pair is not yet captured.)*

#### Stage 7 — FIP-27 (D2)

- [ ] **T7.1** Upgrade **lake-reading connectors + storage plugins first**.
- [ ] **T7.2** Then the tiering service. *Script pin and compose mount verified on 1.0.0 by `CHG-301`; what remains open is the **running** job's version, which only a live tiering run can show. Scheduled with full preconditions and the 4-step revert in `2026-09-22-fluss-1.0-native-adoption.md` § "Carried-in gaps" (~35–45 min, on an idle cluster).* *Precondition found 2026-09-23: run this stage against tables created **after** the cluster had `datalake.format` — a table created before that can never be altered into tiering. Fluss 1.0.0 throws `InvalidAlterTableException` ("tables that were created before the Fluss cluster enabled datalake", `fluss-client/…/LakeEnableTableITCase.java:120-127`), so the only repair is drop + recreate. On the dev stack every datalake-enabled table in the corpus is stuck disabled for exactly this reason — which is why the tiering smoke cannot run there without recreating `raw_table_1`, and per T7.4's note that run would also refresh a lake table a 0.9.1 rollback may need. Prove tiering on the cutover cluster (or on a freshly created table) instead.*
- [ ] **T7.3** Then the cluster.
- [ ] **T7.4** **No new lake table** until the readers are upgraded. *(standing constraint)* *Still holding: zero clean tables exist. Note for whoever runs the tiering smoke — it will create the first clean table for `raw_table_1` (its lake table is stale, dated 2026-08-31 vs the cluster's 2026-09-17), which is permitted because every reader is already on 1.0.0, but it does impair a 0.9.1 rollback of that table's lake data.*

**Tiering verification on 1.0.0 — PASS (2026-09-23).** A fresh `datalake.enabled=true` table tiered **8 lake
objects / 136,222 B** into a **fresh R2 prefix**, **0 tiering errors**; `raw_table_1` and its stale lake path
were never touched (no archiving). Evidence: `logs/soak/tiering-1.0-verify-20260923T054824Z/`. Runner (now tracked): `code/01_platform/04_scripts/tiering-1.0-verify.sh`.
Closes the native plan's entry condition, `FACT-011`'s 1.0.0 re-verification, the `W4` jar's open test, and
`T7.2`'s "running job's version" (the job ran `fluss-flink-tiering-1.0.0.jar`).
**Caveat (native plan line 152 rule):** the run needed two window-only tablet overrides — task interval `1m`
and segment file size `1mb` (tiering copies only *rolled* segments). Both reverted; a green run does **not**
extend the certificate. **Source facts:** the interval is *not* dynamically settable in 1.0.0
(`DynamicServerConfig` = fixed keys + the `datalake.` prefix), and `table.datalake.enabled` **is** alterable —
the genuinely create-only option is `table.datalake.format` (corrects "ALTER stays create-mostly").
**Defect found and fixed:** `--force-recreate <service>` without `--no-deps` recreates the dependency chain;
that renamed the coordinator to `<hash>_01_docker-fluss-coordinator-1`. Restored to canonical, 0 strays.

#### Stage 8 — Docs, evidence, change record (Tier 5)

- [x] **T8.1** Done 2026-09-22: `CHG-299/300/301.md` all exist with the `CHG-297` fields filled.
- [ ] **T8.2** Keep the **23 frozen records and `CHG-298` byte-identical**. *(standing constraint — history is append-only)*
- [x] **T8.3** Tier 5 sweep, part 1 — image READMEs, the probes `core-site.xml`, `ing-tcp001/README.md`, `ddl_apply.py`, `fetch-jars.sh` headers, and `.env`/`.env.example` comments. *Done during Stage 2. Three of these made **behavioural** claims naming 0.9.1 and were re-verified against 1.0.0 before relabelling (see §2.8).*
- [x] **T8.4** Tier 5 sweep, part 2 (done 2026-09-22, `11376f54`) — the 4 remaining Java comment sites: `DiscontinuityWriter` L311/317, `FlussClientAdapter` L28/271–272, `QuarantineWriter` L208/214, `RawTickWriter` L43.
- [x] **T8.5** Tier 5 sweep, part 3 (done 2026-09-22, `11376f54`) — `code/common/pom.xml` L41/45, `ingestion/pom.xml` L121, `02_compute/README.md` L21.
- [x] **T8.6** Tier 5 sweep, part 4 (done 2026-09-22 — trivial class fixed with evidence, evidence class annotated, history left; see commit) — the 8 `docs/08_implementation/*.md` files plus `01_project/04-decisions.md`, `05-risks-and-assumptions.md`, `02_requirements/05-interfaces.md`, `03_architecture/01-technology-choices.md`, `04_contracts/01-ingestion.md`.
- [x] **T8.7** Tier 5 sweep, part 5 (done 2026-09-22, `11376f54`; plus `32_candle_live.sql`, `02-data-pipeline.md`, contract `:19`) — the 3 storage docs still asserting *"no per-key TTL in 0.9.1"*; 1.0 provides `table.kv.ttl` + `.time-column`.
- [x] **T8.8** SUPERSEDED by decision 2026-09-22: `AC-FLS-017` stays `NOT_IMPLEMENTED` (no dec038 evidence report, `kv.ttl` unused on our tables — flipping it would write an unfunded check); the parenthetical now states the native-TTL truth (`11376f54`). Revisit only with live adoption + test report.
- [x] **T8.9** Revisit the decisions this upgrade touches (done 2026-09-22, `3b0cc87c`: DEC-018/032/040 notes + DEC-052 pin + ASM-008; `client.writer.retries=MAX_VALUE` verified still true in 1.0.0 source) — `DEC-040` was written on the premise that `client.writer.retries` defaults to `Integer.MAX_VALUE`; **verified still true in 1.0.0**, so DEC-040 survives but should say so explicitly.
- [x] **T8.10** Update the remaining enumerated docs (plan §8.6) (done 2026-09-22 — interfaces/tech-choices/ingestion-contract/02-schema/09-swarm/12-compat banner; FACT-011 stays **LIVE** and scoped to 0.9.1 (`docs/ENVIRONMENT.md:177`), which is what its recheck
  recorded; **FACT-018** is a separate fact about the cert-speed tiering override - corrected 2026-09-22 in
  the 84-item audit, where the earlier wording had recorded the wrong disposition; see commit).

#### Stage 9 — Production cutover (D3)

- [x] **T9.1** **Decide the production pin here, not earlier** — weigh Stage 6's numbers and elapsed time against bringing up 1.0.0 now. *(Rule accepted 2026-09-23: with no 0.9.1 baseline persisted (see T6.0), T6.1's numbers are judged against absolute SLO budgets, never a ratio. Bar: p99 inside the documented SLO, no OOM/backpressure/Killed during the probe, probe completes at the target rate. Pin prod inside a scheduled cutover window after T6.1; the pin itself is `image-publish.sh --merge-env`.)*
  *Decision recorded 2026-09-23 (operator): **this host is the production host, and the production pin
  flips to the 1.0-built wrappers.** What that changes here is the pin, not an upgrade: the cluster content
  on this host is already 1.0.0 (`01_docker-fluss-{coordinator,tablet}-1` run `apache/fluss:1.0.0`, moved
  through the FIP-27 order during dev). `images.published.env` still carries the pre-1.0 digests published
  from `origin/main`, and that file is written **only** by `publish-images.yml` (build, push, then
  `digest-pin.sh` resolves the digest and the workflow commits the file). The 1.0 wrappers exist here only
  as local builds `trading-fluss-runtime:0.1.0` / `trading-flink-runtime:0.1.0` (2026-09-22, `make images`)
  and are **not pushed**: this host has no `~/.docker/config.json` and its `localhost:5000` `registry:2`
  container has been `Exited (2)` since 2026-09-21. The publish is therefore owed to CI (a `v*` tag push - `publish-images.yml` triggers on `tags: ['v*']` and `workflow_dispatch` only, never on a branch push) or to an
  explicit `docker login ghcr.io` - **pending, owner: operator** - after which the digest re-resolution and
  the flip are mechanical.
  - 2026-09-23 local-registry route TRIED AND REVERTED: pushing the 1.0-built service images to the local `registry:2` on `localhost:5000` was attempted (all 7 pushed, digests resolved), then undone - the original container's storage was a `/tmp` bind mount whose data was already gone; the replacement used a named volume; all 7 `:1.0.0` tags and that volume were removed. Nothing in the running stack or in `runtime.lock` references `localhost:5000` (`runtime.lock` pins `apache/fluss:1.0.0@sha256:ff461b45...`), so this changes no live state. The durable publish path stays CI on a `v*` tag, or an explicit `docker login ghcr.io`.
  **Rollback target to preserve across that publish** (the file's own pre-1.0 digests at `2bb1d9e4`):
  `FLUSS_IMAGE=ghcr.io/saurabh17111994/trading-fluss-runtime:prod@sha256:80c0e52fc88644bdc44791343f06561e273c3f6bfdcd6c1e3d42a8aa3c8e80ec`
  `FLINK_IMAGE=ghcr.io/saurabh17111994/trading-flink-runtime:prod@sha256:d7490df6e32eb50f29a0245d7a2ae19382343a8a91853bcea6bf6f39fd961211`,
  plus the local wrappers `localhost:5000/trading-{fluss,flink}-runtime:prod` (`acfe030bd1a3`,
  `25619cfb0fbd`). CI pins the mutable `:prod` tag, so a publish overwrites the remote 0.9.1 artifact; these
  recorded digests and the local copies are what keep a rollback possible. `03-rollback.md` should name the
  **new** target at the flip - it is version-free today and not stale.
  **T6.5 ticked with the same evidence pass:** `log.retention.roll-active-segment.enabled` is set nowhere in
  this cluster's `server.yaml`, and the running tablet's `fluss-server-1.0.0.jar` declares in its own
  `ConfigOptions` that it is "Disabled by default" - so the rolling upgrade ran with active-segment rolling
  off, as the constraint requires.**
  **Pre-cutover 0.9/0.9.1 audit (2026-09-23, taken before the pin flip): no live reference to Fluss 0.9.x
  remains anywhere in `code/`.** 1160 tracked files scanned; 125 fluss-related `0.9.x` lines, all classed as
  history comments/READMEs (72), test fixtures (37), or narrative inside originally-dated matrix rows (15),
  plus one in-code message that correctly names both versions; 49 further matches were incidental (ticket
  CSV prices, `go.sum`/`Cargo.lock` third-party versions, OpenObserve `v0.91.5`). Every version-declaration
  site is 1.0.0: `code/pom.xml:31 <fluss.version>`, the six compose/Dockerfile plugin mounts, `fetch-jars.sh`
  (`FLUSS_VERSION="1.0.0"` per verified digest), `ddl-apply/Dockerfile` (`fluss-client/1.0.0`) and
  `.env.example`. The one semantic residual is *this* item's own pin file, whose digests are still the
  pre-1.0 builds - it carries no "0.9" string, and the publish above is what clears it. The version matrix is
  clean: 23 boundaries, `version_matrix_verify.py` OK, a fresh 2026-09-23 row per Fluss boundary at 1.0.0
  with the 0.9.1 rows kept as history. The single code-affecting finding was
  `code/01_platform/04_scripts/fluss-repair/LogScan.py`, whose batch geometry was documented from 0.9 -
  re-verified against 30 real 1.0.0 segments (up to 198 MB, every one walked to its exact file end), so the
  repair path is safe on 1.0 and only the docstring needed the version note. Raw inventory (gitignored):
  `logs/fluss09-audit-20260923/`.**
  Not covered by this pass: whether the ~30 in-code "0.9.1 cannot X" rationale comments are still accurate
  on 1.0 - they are justifications, not pins, and their feature classes were adjudicated in A1-A6
  (`2026-09-22-fluss-1.0-native-adoption.md`, DEC-053), so nothing can break from leaving them.**
- [x] **T9.2** Push the images, then resolve the manifest digest with `digest-pin.sh <repo>:<tag>` — a **pushed** digest, never a local image ID and never the stock upstream tag. *(Done 2026-09-23: the tag `v2026.09.23` at `afa2fc4d` fired run `35770015626`; fragment commit `e2590815` records all seven digests with `source-commit: afa2fc4d296a`, and all 7 resolve anonymously against GHCR — no credentials, no docker, no local image ID.)*
- [x] **T9.3** `make images`, then gate step [18/19] for content stamps and staleness. *(Done 2026-09-23: gate step [18/19] built all eight images with content stamps and staleness checks in `logs/soak/monday-gates-20260923-001918/`; the publish above recorded the digests.)*
- [x] **T9.4** Done 2026-09-22: zero `.jar` binds in `docker-stack.yml`; version flows via `FLUSS_IMAGE` only.
- [ ] **T9.5** **A4 on the five money-path KV tables: recreate with `replication.factor=2` +
  `table.kv.standby-replica.enabled=true`, then rebuild state from audit.** *(Decided 2026-09-23 and deferred
  to this window, because there is no in-place path: `ALTER` on `table.replication.factor` is refused by name,
  and `rf=2` is rejected at CREATE on dev's single tablet, so the shared corpus cannot carry the option either.
  **Owner:** the operator runs it inside the T9.1 window; the builder delivers the overlay and the rehearsal.
  **BLOCKED on a prerequisite that does not exist.** Dropping the five tables destroys live state, and the
  rebuild-from-audit path that would restore it was measured absent on 2026-09-23: every mechanism in the repo
  runs KV -> job (rehydration) or lake -> local parquet (`r2-restore.sh`), never audit -> KV. Building that path
  is a new subsystem, and it is owed to the DR requirement (`04-dr-plan.md:26`) regardless of A4 — so A4 is a
  consumer of it, not a reason to build it. See the adoption plan's step 2. **Definition of done, once that
  prerequisite exists:** both options in force on all five tables, asserted through `Admin.getTableInfo` — the
  same call `SafetyHaltJob` uses to refuse startup; ZK shows a non-empty `standby_replicas` per bucket; a leader
  kill with the dead tablet left `exited` moves leadership and restores reads inside a bound agreed before the
  window, with **zero row loss**; and a documented way back that re-creates as `rf=1` and rebuilds, which costs
  a second window. **Not free and not instant:** the drop is
  destructive and the rebuild is the recovery path, so trading halts first — and the halt mechanism's own table
  is one of the five. Rehearsal plan: `2026-09-22-fluss-1.0-native-adoption.md`.)*

#### Workflow / delivery

- [x] **W1** Approve and split the commits for Stages 0–2. *Proposed: F1 drift fix (own commit) · the Fluss bump itself · the plans.* *(Accepted 2026-09-23: the bump already landed, so the remainder split as the F1 drift fix (`dcc88d6b`, with `CHG-303` + `DEC-054`) then this plan-bookkeeping commit.)*
- [x] **W2** SUPERSEDED 2026-09-22: same finding as T2.18 — already in tree, split no longer possible. Breach recorded.
- [x] **W3** Done 2026-09-22: all three `docs/plans/2026-09-22-*` files are tracked (this plan, the
  tracer archive record, the native-adoption backlog) - the audit that ticked it found nothing untracked.
- [x] **W4** Delete `logs/fluss-1.0-superseded-jars-20260922/` once 1.0.0 is certified — **6 of 7 deleted 2026-09-22** (165 MB reclaimed; digests recorded in the commit that did it). All six had a verified, pinned 1.0.0 replacement on disk and no tracked-file reference. **One file deliberately held:** `fluss-fs-hadoop-shaded-0.9-SNAPSHOT.jar`. It 404s on every public repository, so deleting it is irreversible, and `flink-runtime/README.md` records a *live* residual risk against it — 106 shared entries differ in bytes from the published `fluss-fs-s3` + `fluss-fs-hdfs` pair, and "the only definitive test is a running tiering job — a live drill, not covered by the offline suites". That drill is parked (tiering on 1.0.0 unverified, `FACT-011`), so the jar is the last reference for an unverified path. **Release condition:** delete once the tiering smoke has run green. *(2026-09-23: the held file's blocker — a green 1.0.0 tiering run — is cleared; deletion pending operator.)* *(2026-09-23: DELETED — the release condition (a green 1.0.0 tiering run) is met. Its identity is preserved here since it 404s everywhere: `fluss-fs-hadoop-shaded-0.9-SNAPSHOT.jar`, sha256 `21ec7c90e7c8b3deb8696b8b2d724089f9a231877606c1a2eb8aa8521d88485e`, 22196013 B. No compose mount referenced it; only README/plan prose does.)*

---

## Overview

**Requested outcome.** Move the platform from Fluss `0.9.1-incubating` to Fluss `1.0.0` — same Flink
line (2.2.1), same ZooKeeper (3.9.2), same Java (17.0.19) — and end with every version-carrying
artifact, pin, evidence record, and doc reconciled to 1.0.0. The upgrade retires four documented
limitations our own records have been carrying: no per-key KV TTL, no control-plane HA, no bounded
disk-write path, and no native local-vs-remote read preference.

**Acceptance criteria** — all nine hold before the production cutover:

1. `make gate` is green end to end (19 steps, immutable numbering) on the bumped tree.
2. `make pin-check` is green — all six checks, including `version_matrix.yaml` shape and
   `runtime.lock` digest discipline.
3. Every pinned jar in `fetch-jars.sh` (both images) is re-verified against a **new** SHA256 recorded
   from the published 1.0.0 artifact; no pin is carried over from 0.9.1.
4. `make fluss-image` and `make flink-image` build, and both `--verify` passes confirm the staged
   bundles match their pins.
5. The five version-compatibility evidence rows (`VM-FLUSS-SRV-005`, `VM-FLUSS-CLI-006`,
   `VM-FLUSS-CONN-007`, `VM-FLINK-SRV-003`, `VM-IMAGES-012`) are re-recorded against 1.0.0 with a
   fresh `date` — not edited in place as if the 0.9.1 evidence were still current.
6. The four behaviour changes below are each verified on a scratch cluster, not inferred:
   new-partition startup position, the removed `currentOffset` gauges, the `max.bucket.num` default,
   and `remote.data.dir` → `remote.data.dirs`.
7. Writer p99 and the 50k ticks/s baseline are re-measured on 1.0 and recorded beside the 0.9.1
   numbers rather than replacing them.
8. A new `CHG-299` records the change with `compatibility_class`, `savepoint_impact`, and
   `rollback_behavior` filled in. No existing change record is edited — the 23 frozen records and
   `CHG-298` stay byte-identical.
9. The production digest in the Swarm environment is a **pushed** manifest digest resolved by
   `digest-pin.sh`, not a local image ID and not the stock upstream tag.

**Non-goals.**

- **No Flink upgrade.** 1.0.0 ships `fluss-flink-2.2`, matching the pinned Flink 2.2.1. Flink stays
  at 2.2.1 and `flink.version` does not change.
- **No ZooKeeper retirement.** 1.0.0 still requires ZooKeeper; the KvStore/Raft work is roadmap.
  `VM-ZK-013` stays open.
- **No multi-replica KV durability.** `table.kv.standby-replica.enabled` is failover *speed*, is
  disabled for pre-existing tables, and needs an explicit `ALTER TABLE`. It is not a durability
  feature and is not treated as one here.
- **No new 1.0 surface we have no use for.** C++/Python/Rust clients, Hudi, Google Cloud Storage,
  Tencent COS, the five new catalogs, RoaringBitmap SQL functions, the InfluxDB reporter, and the
  Fluss Gateway (preview, read-less) are out of scope. Nothing in this plan enables them.
- **No `AppendResult` offset fix.** 1.0.0's `AppendResult` is still an empty class (`P1-223` stays
  open) and `client.writer.retries` still defaults to `Integer.MAX_VALUE` (`DEC-040` stays valid).

## Decisions closed before this plan started (2026-09-22)

These three were decided by the operator and are **inputs**, not open questions. Every stage below
assumes them.

| # | Decision | Consequence carried through this plan |
|---|---|---|
| D1 | **Archive the 7 uncommitted fetch-path patches, then delete them.** | Stage 0 archives first. The `diag/fetch-cycle-timing` branch is committed and its *findings* written down before the tree is abandoned. 1.0.0's `client.scanner.log.read-preference` (`local-first` default / `remote-first`), `client.remote-file.download-thread-num`, and `client.scanner.io.tmpdir` replace what the tracer was measuring by hand. |
| D2 | **Take FIP-27 (the lake clean schema) in this upgrade.** | Stage 7 executes the documented component order — lake-reading connectors and storage plugins → tiering service → cluster — and holds "create no new lake table" until readers are upgraded. |
| D3 | **Certify on 1.0.0 now; decide the production pin at cutover.** | Stages 1–8 pin `1.0.0`. Stage 9 is the only place the production digest is chosen, and it happens after Stage 6's perf results exist. |

## Context

- **Upgrade notes (authoritative):** `website/docs/maintenance/operations/upgrade-notes-1.0.md` in the
  Fluss `v1.0.0` tag. Three sections drive tasks in this plan: Authorization Changes (ACL `ALL`),
  Cluster Configuration Changes (`datalake.enabled`, active-segment retention rollout, AWS SDK v2),
  and Lake Table Schema Changes (FIP-27).
- **Source of the escape claims:** `FlinkSourceEnumerator.java` at `v1.0.0` — line 610 sets
  `newDiscoveryOffsetsInitializer = OffsetsInitializer.earliest()`; lines 165–167 document the
  FLIP-288 rationale. The field does not exist at `v0.9.1-incubating`.
- **Two Fluss trees on this host:**
  - `/home/saurabh/Jupyter_notebook/Flink_Fluss_Infrastructure/fluss/Fluss_v_0.9.1` — git checkout on
    branch `diag/fetch-cycle-timing`, at tag `v0.9.1-incubating`, carrying the 7 uncommitted patches.
    Tags `v1.0.0` and rc1/rc2/rc3 are fetched here. **Stage 0 must not destroy this.**
  - `/home/saurabh/Jupyter_notebook/Flink_Fluss_Infrastructure/fluss/fluss-_1.0.0` — the **binary
    distribution** (`fluss-1.0.0-bin.tgz`, 32 files, 532 MB). A different artifact class from a source
    checkout; its file count is not comparable.
- **Verified 1.0.0 source:** `/tmp/fluss-verify/fluss-1.0.0` — extracted from
  `fluss-1.0.0-src.tgz` (5,140,034 bytes), SHA512 verified against the published `.sha512`, and
  2,908/2,908 Java files identical to tag `v1.0.0`. Safe to re-download if `/tmp` is cleared:
  `https://downloads.apache.org/fluss/fluss-1.0.0/`
- **Release facts:** Maven Central artifacts published 2026-09-18; tag `v1.0.0` created 2026-09-21;
  `refs/heads/release-1.0` exists (a 1.0.1 would be a cherry-pick, not a rebase).
  `fluss-flink-2.2:1.0.0` and `apache/fluss:1.0.0` both exist.
- **The evidence base this plan re-certifies:** 349 records in `logs/` (append-only — new records are
  added, 0.9.1 baselines are superseded not rewritten), `code/01_platform/04_scripts/versions.pin`
  (7 JAR/image digests), and the 15-row `version_matrix.yaml`.

## Review handoff

- **Approach:** treat this as a **re-certification project with a mechanical pin bump inside it**, not
  as a version bump. Stages 1–2 are the bump (~34 files, mostly literal). Stages 3–7 are the work.
- **Assumptions (each is verified in a stage, none is treated as settled):**
  1. That the iceberg plugin classloader still cannot see `plugins/s3/`, so the derived Fluss runtime
     image is still required. Verified in Stage 2.4 — the *jar completeness* argument does not settle
     it, because 1.0.0's `fluss-fs-s3-1.0.0.jar` is self-contained (43 MB) yet the plugin dirs remain
     separate.
  2. That P1-052's shader exclusion is still correct, on the strength of "fluss-client bundles a full
     slf4j-api 1.7 copy (verified in fluss-client-0.9.1)". That verification is stale. Re-run in
     Stage 1.6.
  3. ~~That `hadoop-mapreduce-compat-2.8.5.jar` is still required and still clash-free~~ —
     **CLOSED 2026-09-22, still required.** Evidence, all from `fluss-fs-s3-1.0.0.jar` and
     `fluss-fs-hdfs-1.0.0.jar` downloaded from Maven Central:
     - `org/apache/hadoop/mapreduce/lib/input/FileInputFormat` — **0 occurrences in both jars**,
       and no `org/apache/hadoop/mapreduce/` package at all in the s3 jar's top-level tree. The
       class the compat jar supplies is still genuinely absent, so the jar is still needed.
       Not obviated by 1.0's self-contained `fluss-fs-s3`.
     - Bundled Hadoop is **3.4.3**, read from the jar's own `META-INF/NOTICE`
       (`org.apache.hadoop:hadoop-common:3.4.3`; also hadoop-auth/aws/annotations 3.4.3). Not
       inferred from the release notes.
     - `COMPAT_SHA256` is **unchanged**: the derivation source
       (`hadoop-mapreduce-client-core-2.8.5.jar`) and the recipe are both untouched by the upgrade,
       so the derived bytes cannot differ. The Stage 2.5 `--verify` run remains as confirmation.
     This also retro-settles a documentation sweep: **5 stale `3.3.x` claims** were found, of which
     3 are editable (`flink-runtime/fetch-jars.sh:28`, `docker-compose.yml:596` + `:712`,
     `fluss-plugins/iceberg/README.md:27`) and were corrected to 3.4.3. The other 2 —
     `docs/05_deployment/change-records/CHG-179.md:72` and
     `docs/06_operations/07-lake-archive-ops.md:168` — are **append-only historical records and were
     deliberately left untouched**; they describe what was true when written.
  4. ~~That the S3A socket-timeout mapping survives the AWS SDK v1→v2 move~~ —
     **CHECKED 2026-09-22, no risk.** Newly identified during this plan's execution, not in the
     original brief. The "R2 stall containment" comment in `docker-compose.yml` asserts that
     `fs.s3a.connection.timeout` maps to the SDK socket read timeout and that
     `fs.s3a.socket.timeout` is not read. 1.0 moves S3A to SDK v2 (`fluss-fs-s3-1.0.0.jar` holds
     6,659 `software/amazon/awssdk/` entries and **0** `com/amazonaws/` entries), so those
     assertions could have silently reverted. They did not:
     - `org.apache.hadoop.fs.s3a.impl.AWSClientConfig` still reads **both**
       `fs.s3a.connection.timeout` and `fs.s3a.connection.establish.timeout`, converts them to
       `java.time.Duration`, and applies them to the SDK v2 `ApacheHttpClient` /
       `ClientOverrideConfiguration` builders.
     - `fs.s3a.socket.timeout` appears **nowhere in the jar**, so "must not be set" still holds.
     *Strength of this evidence:* the keys being consumed and wired into the SDK v2 HTTP client is
     shown from the class constant pools. The exact arithmetic was **not** decompiled, so "same
     values as 3.3.x" is a strong inference, not a byte-level proof. Stage 5's B4 R2 read is where
     it becomes measured.
- **Residual risk this plan cannot close:** the 1.0.0 release is ~4 days old with rc1/rc2/rc3 behind
  it. Adequate for pre-production certification; the Stage 9 decision point is where that is weighed
  for a live-money deployment.
- **Known limitation of this plan's own evidence:** Stage 5's predictions about which tests break are
  derived from reading the files, not from running them. Stage 3 is where they become measured.

---

## The bump surface — 60 files name the old version, 34 need action

Measured by sweep: 60 tracked files reference `0.9.1`, excluding `logs/`, `target/`, and the frozen
`docs/05_deployment/change-records/`. They sort into five tiers. **Tiers 1–4 are the change. Tier 5 is
comment and documentation accuracy, and is deliberately separated so a reviewer can see what actually
affects behaviour.**

### Tier 1 — Pins (7 files). Nothing else can be re-verified until these move.

| File | Exact site |
|---|---|
| `code/pom.xml` | L31 `<fluss.version>`. The **only** Maven edit — `code/common/pom.xml`, `code/02_services/01_ingestion/pom.xml`, `code/02_services/02_compute/pom.xml`, `code/02_services/06_execution_gateway/pom.xml` all inherit `${fluss.version}` and need no version edit. |
| `code/01_platform/04_scripts/versions.pin` | `FLUSS_VERSION`, `FLUSS_CLIENT_JAR_SHA256`, `FLUSS_FLINK_CONNECTOR_JAR_SHA256`. Both digests must be re-recorded from the 1.0.0 artifacts. |
| `code/01_platform/04_scripts/version_matrix.yaml` | 4 rows, 18 references: `VM-FLUSS-SRV-005` (L178), `VM-FLUSS-CLI-006` (L253), `VM-FLUSS-CONN-007` (L295), `VM-IMAGES-012` (L456); plus `VM-JAVA-001` prose at L40 and L55. Guarded by `version_matrix_verify.py` (pin-check [1/6]). |
| `code/01_platform/01_docker/runtime.lock` | `FLUSS_IMAGE` (digest), `FLUSS_VERSION`, and the version-matrix comment at L13/L18. |
| `code/01_platform/01_docker/runtime.lock.example` | `FLUSS_IMAGE` (tag form), `FLUSS_VERSION`, same comment. |
| `code/01_platform/01_docker/.env` | L15 `FLUSS_IMAGE`. |
| `code/01_platform/01_docker/.env.example` | L24 `FLUSS_IMAGE`. |

### Tier 2 — Image build (6 files). The jar chain.

| File | Exact site |
|---|---|
| `code/01_platform/01_docker/fluss-runtime/fetch-jars.sh` | `FLUSS_VERSION`; the 2-entry `PINS` table (`fluss-fs-s3`, `fluss-fs-hdfs`); the header block that justifies excluding the `fluss-fs-hadoop-shaded` SNAPSHOT. **Its SHA256s must match the Flink script's for the same two jars** — that identity is deliberate and is what keeps the two images from drifting. |
| `code/01_platform/01_docker/flink-runtime/fetch-jars.sh` | `FLUSS_VERSION`; the 6-entry `PINS` table; `MAPREDUCE_VERSION` stays 2.8.5 and `COMPAT_SHA256` is **not** naively re-pinned (see Stage 2.5). |
| `code/01_platform/01_docker/fluss-runtime/Dockerfile` | `FROM apache/fluss:0.9.1-incubating@sha256:65f5513b…`; 2 `COPY` lines into `/opt/fluss/plugins/iceberg/`. |
| `code/01_platform/01_docker/flink-runtime/Dockerfile` | 5 `COPY` lines into `/opt/flink/lib/`, plus the `hadoop-mapreduce-compat-2.8.5.jar` COPY. `ENABLE_BUILT_IN_PLUGINS=flink-s3-fs-hadoop-2.2.1.jar` is Flink-scoped and does **not** change. |
| `code/01_platform/01_docker/ddl-apply/Dockerfile` | L53 `ARG EXPECT_FLUSS=0.9.1-incubating`; L93–94 hardcoded `COPY --from=builder` paths. **This file does not auto-derive from `versions.pin`** — it needs a real edit and is easy to miss. |
| `code/01_platform/01_docker/docker-compose.yml` | **18 mount lines across 4 services**: L346–348 (fluss-coordinator), L458–460 (fluss-tablet), L585–590 (flink-jobmanager), L701–706 (flink-taskmanager). |

### Tier 3 — Runtime wiring (4 files). Host scripts that name a jar or a version.

| File | Exact site |
|---|---|
| `code/01_platform/04_scripts/tiering-start.sh` | L21 `TIERING_JAR=/opt/flink/lib/fluss-flink-tiering-0.9.1-incubating.jar`. |
| `code/01_platform/04_scripts/eod_controller.py` | L35 `os.environ.get("FLUSS_VERSION", "0.9.1-incubating")` — a hardcoded fallback default, not a comment. |
| `code/01_platform/04_scripts/stage-soak-e2e.sh` | L261 comment and L276 the `for jar in` loop that names `fluss-fs-s3-0.9.1-incubating.jar` and the HDFS sibling. |
| `code/01_platform/04_scripts/digest-pin.sh` | L5–6 usage examples. Comment-only, but they *teach* the wrong digest command — update so the next operator does not pin a stale image. |

### Tier 4 — Tests that guard the old version (4 files).

| File | Sites | Predicted effect |
|---|---|---|
| `tests/test_fluss_runtime_image.py` | L48, L54, L60–61, L87, L91, L233 | **Fails.** Asserts `FLUSS_VERSION = "0.9.1-incubating"`, the pinned `apache/fluss:0.9.1-incubating@sha256:…` base, both jar names, and (L233) that the `fluss-fs-hadoop-shaded` SNAPSHOT is absent. The last assertion is the one that *keeps* the SNAPSHOT drop honest — it must be rewritten, not deleted. |
| `tests/test_flink_runtime_image.py` | L46, L57–61, L85, L90, L274 | **Fails.** Same shape, 5 jar names plus the `MAPREDUCE_VERSION` capture. |
| `tests/test_fluss_probes.py` | L677, L679, L695 | **Fails** where it asserts the S3/HDFS jar names and the SNAPSHOT's absence. |
| `tests/test_image_staleness_check.py` | L67 | **Verify.** Uses `apache/fluss:0.9.1-incubating` as a fixture image ref inside a synthetic compose; likely version-agnostic. Confirm before editing. |

### Not a task — checked and cleared

| Item | Why it needs nothing |
|---|---|
| `code/01_platform/04_scripts/pin-check.sh` | Check [4/6]'s strict regex is `^FLUSS_VERSION=[0-9]+\.[0-9]+\.[0-9]+([-+.][0-9A-Za-z_-]+)*$`. `1.0.0` matches. |
| ~~`code/01_platform/04_scripts/pipeline-lib.sh`~~ | **WRONG — this is a task site. See C1/C2 in Stage 2.** The L129–148 guards (rejecting duplicate `org.apache.fluss.*` classes and Fluss ServiceLoader descriptors in `compute.jar`) *are* version-agnostic and need nothing — that much was right. But the same file also carries `pipeline_validate_compose_bind_sources()` (B7) at L156–170, a 7-entry list mirroring the compose jar mounts verbatim, **including the SNAPSHOT**. Clearing the whole file on the strength of one function was the error. |
| `code/01_platform/04_scripts/docs_audit.py` | C7 reads `FLUSS_VERSION=(\S+)` and rejects only `latest`/`TO_BE_PINNED`. Version-agnostic. |
| `tests/test_09_stack.py` | No version reference at all — it asserts service names, volumes, `FLUSS_PROPERTIES`, and Swarm secrets. **An earlier reading of mine said this would fail; it will not.** |
| `tests/test_ddl_apply.py` | Uses `0.9.1-incubating` as a synthetic `~/.m2` fixture string and round-trips it. Version-agnostic; updating it is cosmetic, not required. |
| `tests/test_pin_check.py` | Asserts `FLUSS_VERSION pinned` is printed. Version-agnostic. |
| `tests/test_tiering_restart_guard.py` | Asserts `fluss-flink-2.2` / `fluss-client` keep `provided` scope. Version-agnostic. |
| `docker-compose.bench.yml`, `.p10.yml`, `.p7.yml`, `.soak.yml` | **0 jar references.** No edit. |
| `code/01_platform/01_docker/docker-stack.yml` | **0 jar references** — production uses `image: ${FLUSS_IMAGE:?…}` in 4 services and relies entirely on the image. No edit; the production change is the digest value only. |
| `code/common/src/main/java/com/trading/common/version/VersionGate.java` | Has **no** allow-list; it only rejects `latest`/`SNAPSHOT`. `1.0.0` passes automatically. |
| `code/02_services/02_compute/dependency-reduced-pom.xml` | Generated by maven-shade. Regenerated by the build; never hand-edited. |
| `docs/plans/2026-08-25-*.md`, `docs/plans/2026-09-16-*.md` | Dated plan records. Superseded by this file, not rewritten. |

### Tier 5 — Comments and docs (accuracy only, no behaviour)

Every Java reference to `0.9.1` is **comment-only** — verified by reading each site. No Java code
changes in this upgrade.

| File | Site | What is now wrong |
|---|---|---|
| `01_ingestion/.../discontinuity/DiscontinuityWriter.java` | L311, L317 | Claims the release is unbounded in Fluss 0.9.1; claims `TableWriter` has no `close()`. |
| `01_ingestion/.../FlussClientAdapter.java` | L28, L271–272 | `Real Fluss API (0.9.1-incubating)`; `P1-223` cites `javap` against `fluss-client-0.9.1-incubating`. |
| `01_ingestion/.../quarantine/QuarantineWriter.java` | L208, L214 | Same unbounded-flush and no-`close()` claims. |
| `01_ingestion/.../write/RawTickWriter.java` | L43 | `version-gated on Fluss 0.9.1-incubating`. |
| `04_scripts/fluss-probes/FlussPrefixReader.java` | L151 | `fluss-client 0.9.1-incubating — the interface has no close() method`. |
| `04_scripts/fluss-probes/hadoop-conf/core-site.xml` | L18 | `against fluss-fs-s3-0.9.1-incubating.jar`. |
| `04_scripts/flush_guard.sh` | L6, L95 | Unbounded-`await` claim. |
| `code/common/pom.xml` | L41, L45 | Prose about the 0.9.1-incubating client being self-contained shaded. |
| `code/02_services/01_ingestion/pom.xml` | L121 | P1-052's justification, including the now-stale slf4j verification. |
| `code/02_services/02_compute/README.md` | L21 | Checklist line citing Fluss 0.9.1-incubating. |
| `fluss-runtime/README.md`, `flink-runtime/README.md`, `fluss-plugins/iceberg/README.md` | — | Image documentation. |
| `04_scripts/ing-tcp001/README.md` | — | Probe documentation. |
| `docs/08_implementation/*.md` | 8 files | `01-foundation`, `02-schema-storage`, `04-signal-job`, `05-execution-core`, `09-production-swarm`, `11-testing-and-release`, `12-version-compatibility-evidence`, `RELEASE_EVIDENCE_2026-08-21`. |
| `docs/01_project/04-decisions.md`, `docs/01_project/05-risks-and-assumptions.md` | — | `DEC-018/021/032/038/040`, `ASM-NFR-006`. |
| `docs/02_requirements/05-interfaces.md` | — | `ASM-IF-002/004`. |
| `docs/03_architecture/01-technology-choices.md` | — | Architecture claims about Fluss. |
| `docs/04_contracts/01-ingestion.md` | — | Ingestion contract. |

Also in Tier 5, and **not** comment-only:

| File | Issue |
|---|---|
| `02_services/01_ingestion/.factorypath`, `02_services/06_execution_gateway/.factorypath` | Eclipse-only artifacts hardcoding `M2_REPO/org/apache/fluss/fluss-client/0.9.1-incubating/fluss-client-0.9.1-incubating.jar`. If Eclipse is unused these are dead files and should be **deleted, not updated**. Ask before deleting. |
| `docs/02_requirements/02-functional/02-storage.md:200`, `docs/04_contracts/02-storage.md:56`, `docs/03_architecture/00-arch-overview.md:187` | Assert "no per-key TTL in Fluss 0.9.1". 1.0.0 implements it. |
| `AC-FLS-017` | Currently `NOT_IMPLEMENTED`. This is an acceptance-criterion **status change**, not a doc edit. |

---

## Stages

### Stage 0 — Close the fork before anything else (D1)

The only irreversible step in the plan, and the only one that can destroy work.

1. ~~Commit the `diag/fetch-cycle-timing` branch~~ — **SKIPPED, decided 2026-09-22.** Archiving was
   chosen instead of a commit, so `fluss/Fluss_v_0.9.1` stays clean at `v0.9.1-incubating` with a
   dirty tree, and no commit enters a third-party Apache repo. The work is protected by step 2.
2. **DONE 2026-09-22.** Archived to `logs/fluss-fetch-tracer-20260922/`
   (`fetch-cycle-timing.patch` 27,076 bytes, `FetchCycleTracer.java` 74 lines, `EmitTracer.java`
   47 lines, `fluss-context.md`, `PROVENANCE.txt`) with a tracked record at
   `2026-09-22-fluss-fetch-tracer-archive.md`. Verified two ways: the patch applies cleanly to a
   pristine `v0.9.1-incubating` worktree and reproduces `7 files changed, 251 insertions(+), 2
   deletions(-)`, and `git apply --check --reverse` succeeds against the live tree.

   **The findings are NOT recorded** — the artifact is instrumentation, not a report. If those
   conclusions were never written down, `client.scanner.log.read-preference` now answers the question
   the tracer was asked. Recorded plainly rather than reconstructed.

   The instrumented files were:
   `LogFetcher` +90, `LogScannerImpl` +49, `LogFetchBuffer` +35, `CompletedFetch` +13,
   `FlinkSourceSplitReader` +33, `FlinkRecordEmitter` +22, `FlinkRecordsWithSplitIds` +11.
3. Create the 1.0 tree as a **worktree**, never a checkout:

   ```
   git -C fluss/Fluss_v_0.9.1 worktree add ../Fluss_v_1.0.0 v1.0.0
   ```

   `git checkout` would put the working tree on top of the diagnostic branch.
4. Confirm `fluss/Fluss_v_0.9.1` is still on `diag/fetch-cycle-timing` at `v0.9.1-incubating` with a
   clean tree, and that `fluss/Fluss_v_1.0.0` is at `v1.0.0`.

**Exit:** both trees exist and are independently correct. Nothing lost.

### Stage 1 — Bump the pins (Tier 1)

1.1 `code/pom.xml` L31 → `<fluss.version>1.0.0</fluss.version>`. Nothing else in Maven.

1.2 Collect the new digests **from the published artifact, not from a mirror or a pasted value**:
`fluss-fs-s3`, `fluss-fs-hdfs`, `fluss-flink-2.2`, `fluss-flink-tiering`, `fluss-lake-iceberg`,
`fluss-client`. Record each with the command that produced it.

1.3 `versions.pin`: `FLUSS_VERSION=1.0.0`, plus `FLUSS_CLIENT_JAR_SHA256` and
`FLUSS_FLINK_CONNECTOR_JAR_SHA256` re-recorded. Leave the two pinned Flink JAR digests and all
non-Fluss pins untouched.

1.4 `version_matrix.yaml`: update the four Fluss rows.

   > **CORRECTION (2026-09-22, execution).** This step originally read *"Set every `date` to today and
   > move the status forward honestly."* **Do not do that.** `date: 2026-08-24` is when the 0.9.1
   > evidence was *captured*, and the file's own header forbids populating `result` /
   > `observed_behavior` / `limitations` / `date` from anything but an approved artifact — setting it
   > to today would claim evidence that does not exist. The row was instead moved forward the honest
   > way: `proposed_version` → the 1.0.0 target, `compatibility_class` → `UNKNOWN` (the header's own
   > rule: *"Every row's `compatibility_class` is UNKNOWN until the capability tests run and pass"*),
   > with an explicit RE-VERIFICATION PENDING note in `evidence_source`. `source_artifact`, `fixture`,
   > `scenario`, `result`, `observed_behavior`, `limitations`, `date` and `status` are all left
   > untouched as the 0.9.1 record. `date` becomes 2026-09-22 in Stage 4, when real 1.0 evidence
   > exists.

   `VM-FLUSS-SRV-005`'s limitations block is full of 0.9.1 findings (`Admin.alterTable` rejecting
   `table.log.ttl`, `log.retention.ms` silent no-ops, lake re-enable being create-only). Each needs
   re-testing against 1.0 before that row's `result` is rewritten, because 1.0 changes the answer for
   some of them.

1.5 `runtime.lock` + `runtime.lock.example`: `FLUSS_IMAGE`, `FLUSS_VERSION`, and the version-matrix
comment. `.env` L15 and `.env.example` L24: `FLUSS_IMAGE`.

1.6 **Re-verify the stale justification behind P1-052** — **DONE 2026-09-22, both claims hold.**
   Run against `fluss-client-1.0.0.jar` downloaded from Maven Central:
   - **slf4j: still correct.** 38 `org/slf4j/**` entries, the full API tree (`org/slf4j`,
     `event/`, `helpers/`, `spi/`), `Logger.class` + the 1.7-only `spi/LocationAwareLogger.class`
     present, and **no `org/slf4j/impl/`** binding. Every entry is dated `2022-02-08 13:31` — the
     slf4j-api 1.7.36 release date. So the shaded fat jar would still collide and the
     `org/slf4j/**` exclusion stays.
   - **`.dat`: still correct.** fluss-client 1.0.0 ships **zero** `.dat` files, so "only log4j-core
     contributes a `.dat`" holds and the P1-053 correction is still safe.

   *Method note worth keeping:* the first `.dat` check reported a hit and looked like a broken claim.
   It was `META-INF/services/…jackson.**databind**.Module` — the substring `.dat` inside `databind`,
   matched by an unanchored `grep '\.dat'`. The correct test anchors the extension:
   `awk '{print $4}' | grep -E '\.dat$'`. An unanchored extension grep on a classpath is a trap.

1.7 **NEW, found during Stage 1 execution — `ConfigProvider` is a new ServiceLoader provider.**
   `fluss-client-1.0.0.jar` declares a `META-INF/services/org.apache.fluss.config.provider.ConfigProvider`
   file, alongside the existing `AuthenticationPlugin`, `MetricReporterPlugin`,
   `SecurityTokenReceiver` and three shaded-jackson services. `ConfigProvider` and its
   `common/config/provider` package are new in 1.0, so this provider file did not exist in the tree
   R-060 was written against. R-060's `ServicesResourceTransformer` exists precisely to merge these,
   so the expected outcome is that nothing needs changing — but a ServiceLoader entry that fails to
   merge fails **silently at runtime**, not at build time. Added to the Stage 3 checklist.

**Exit:** `make pin-check` green (all 6) — **PASSED 2026-09-22**. `make gate-fast` — **RED, for two
distinct reasons, neither of them a defect in Stage 1's edits; but one is caused by Stage 1's
*existence*.** Measured 2026-09-22: `gate-fast` now clears `static-check` (0 failures) and stops at
`Makefile:242 → test-audit-r2` (`Error 1`), which runs 1841 tests and reports **8 failures, 18
skipped**. The eight split cleanly, and the split is proven, not inferred:

| Count | Cause | Evidence |
|---|---|---|
| **2** | **Pre-existing**, unrelated to Fluss | Re-run in a clean `HEAD` worktree: `test_compose_form_contract` → `Ran 5 tests … FAILED (failures=2)`, identical assertions. Nothing it names is a file this upgrade touched. |
| **6** | **Stage 1 itself** — pins moved, Dockerfiles not yet | Same three files re-run at `HEAD` → all `OK` (42/19/35 tests). On the working tree → `FAILED (2)`, `(3)`, `(1)`. 2+6 = the 8. |

The 6 are the `Dockerfile ↔ pin` cross-checks doing exactly their job: `versions.pin` and
`fetch-jars.sh` now say `1.0.0` while `fluss-runtime/Dockerfile`, `flink-runtime/Dockerfile` and the
plugin staging still name `0.9.1-incubating`. They cannot pass until **Stage 2** lands. See
"Sequencing correction" below — this stage's exit criterion is unachievable as written.

#### Sequencing correction (2026-09-22): `gate-fast` belongs to Stage 2, not Stage 1

Stage 1 moves the pins; Stage 2 moves the Dockerfiles. The suite cross-checks one against the other
in six places, so **no ordering of Stage 1 alone can satisfy `gate-fast`.** The exit criterion is
therefore moved to the end of **Stage 2**. The two stages stay separate *commits* — that is good for
review — but the intermediate state is expected-red by construction, not a regression to chase. Any
future pin bump in this repo has the same property: the pin and the image rebuild are one atomic
change from the test suite's point of view.

#### Pre-existing blocker: `make static-check` is already red at HEAD (found 2026-09-22)

`make gate-fast` fails at `Makefile:241` → `static-check` (`Makefile:482`), which runs `bash -n` +
`shellcheck -S warning` over every `code/**/*.sh`. Six warnings in **three files this upgrade never
touches**:

| File | Warning | Line(s) |
|---|---|---|
| `code/01_platform/04_scripts/eod-controller-test.sh` | SC2034 `attempt` appears unused | 131 |
| `code/01_platform/04_scripts/test-d-3x20k-run.sh` | SC1090 non-constant `source` | 63 |
| `code/01_platform/04_scripts/test-pipeline-lib.sh` | SC1090 non-constant `source` | 59, 73, 130, 310 |

**Proof it is not this upgrade's doing.** All three files are byte-identical to HEAD
(`git status --porcelain` on each returns nothing), and `static-check` iterates
`find code -name '*.sh'` — a file set this upgrade did not change (no `.sh` added or removed; the new
files live under `logs/`, outside `code/`). Unmodified inputs ⇒ identical shellcheck output ⇒ the
failure existed at HEAD before Stage 1 began.

**Confirmed by re-running `make static-check` on the working tree with Stage 1's edits in place
(2026-09-22):** `rc=2`, `static-check: 1 failures`, and the identical 6 warnings in the identical 3
files. All five shell files this upgrade edited — `flink-runtime/fetch-jars.sh`,
`fluss-runtime/fetch-jars.sh`, `digest-pin.sh`, `stage-soak-e2e.sh`, `tiering-start.sh` — are
**shellcheck-clean**. The deduction and the measurement agree.

*Reporting note:* the first run of this gate was nearly misread as a pass, because the shell wrapper
that launched it ended in `echo`, so the process exited 0 even though `make` had exited 2. When
capturing a gate's status in a wrapper, capture `$?` into a variable and echo it — never let the
wrapper's exit status stand in for the gate's.

**When it broke — datable, and predicted in advance.**

- Clean claim: `docs/plans/2026-09-16-wave36-holistic-measure-repair.md:101` records
  `shellcheck -S warning` reporting *"nothing"*, and `2026-09-18-certification-hygiene.md:197`
  records it clean again.
- The three files were last modified on **2026-09-19**: `777d79b1`
  (*"make the pipeline-lib guard suite assert per function"*), `658de7e2`, `9ed037af`.
- `docs/05_deployment/change-records/CHG-165.md:71` says the `test-pipeline-lib.sh` SC1090 defect
  *"was repaired in its own commit (ad6b07c5)"* — confirmed an ancestor of HEAD — and warns
  *"Reverting ad6b07c5 also re-breaks gate step 1 (SC1090 on test-pipeline-lib.sh)."* The 09-19
  rewrite reached the same state by a different route: `ad6b07c5` (09-15) reattached a detached
  shellcheck directive, and `777d79b1` (09-19) then reintroduced four un-annotated `source "$LIB"`
  sites. No revert commit exists.

**Why it matters beyond this plan:** `static-check` is gate step **[1/19]**, so
`make gate` is red too — this blocks *release certification generally*, not just this upgrade.

**Disposition — REPAIRED 2026-09-22, in its own commit (per the standing rule that an unrelated
failing test is fixed separately, never bundled).** Not folded into any Stage of this plan.
`make static-check` now reports **`0 failures` / rc=0**.

The repair is targeted, not a suppression. Root cause established by experiment: **shellcheck
ignores a directive when the `source`/`.` is one command among several on a `;`-joined line** —
neither `disable=SC1090` nor `source=/dev/null` suppresses it there, while either works once the
source stands alone. All six sites were compound lines or directives detached from their source.
Four were the same defect `CHG-165.md:71` records (wave 17 inserting a preamble between directive and
source), re-introduced by the 2026-09-19 rewrite. A **file-level `disable=SC1090` was rejected
deliberately**: `2026-09-16-wave36-holistic-measure-repair.md:101` names shellcheck's blindness to
the sourced lib as a weakness that *"cannot protect this change"*, so widening it would have turned
the gate green while making it weaker. Following the source instead surfaced no new findings.

Guards unchanged: the suite reports **134 passed / 0 failed** both at `HEAD` and after the repair,
with all 134 verdicts identical line-for-line.

#### Second pre-existing red, REVEALED by repairing the first: `test_compose_form_contract`

`test_compose_form_contract` fails **two** tests — `test_every_site_is_registered` (4 unregistered
`docker compose … config` sites) and `test_no_dead_registry_entries` (1 stale registry row for
`run-monday-gates.sh`). Proven pre-existing in a clean `HEAD` worktree: `Ran 5 tests … FAILED
(failures=2)`, assertion text identical. Every file it names —
`compose_config_redact.py`, `run-monday-gates.sh`, `tests/test_compose_config_redact.py` — is
unmodified at `HEAD`; nothing it names is a file this upgrade touched.

**The shellcheck failure was masking this one.** `static-check` is gate step 1 and aborts the gate on
failure, so `test-audit-r2` was never reached at `HEAD` and these two failures had no way to be seen.
Repairing the first red exposed the second. This is a general property worth remembering here: *a red
early step hides every red behind it, so "the gate was red for one reason" is never a safe claim
until the first red is cleared.*

**Disposition — REPAIRED 2026-09-22, as a second standalone commit.** Not folded into any Stage, and
kept separate from the shellcheck repair too: two unrelated pre-existing defects, two commits.

What the repair actually was — the diagnosis mattered more than the edit:

- **Not** a stale-row bug. Of the four "unregistered" sites, three are **docstring prose** that
  merely *quotes* the form — `compose_config_redact.py`'s module docstring (the "why the render must
  not be logged verbatim" paragraph and its `Usage:` line) and
  `tests/test_compose_config_redact.py`'s docstring. The registry already has a kind for exactly
  this — `TEXT`, *"prose in a message, docstring or log line — no invocation"* — used by 20+ existing
  rows. Registering them `TEXT` restores the contract's own design; the scanner was **not** weakened
  to skip docstrings, which would have blinded it to a real invocation quoted inside one.
- The fourth site, `run-monday-gates.sh:600`, **is** a real invocation and is a legitimate `FLAGS`
  site: it carries both `--env-file` flags, and `-f "$COMPOSE_FILE"` resolves through that file's own
  `COMPOSE_FILE=…/docker-compose.yml` assignment (line 87), which is what `_stack_file()` requires.
  Verified against the checker before registering, so the row could not trade one failure for another.
- The "dead" row was **not** dead by omission — the invocation gained a stage. Its tail changed from
  `config >"$COMPOSE_CONFIG_LOG" 2>&1` to a pipe through `compose_config_redact.py` (the 2026-09-21
  credential-leak fix) with the log written after the filter. The row now pins the *invocation*
  (`-f "$COMPOSE_FILE" config`) rather than the redirect, so it survives the filter being renamed or
  the log path changing.

Result: `Ran 5 tests … OK` (was `FAILED (failures=2)`), and the redact feature's own suite still
passes (`10 passed`) — the contract was re-pointed, not relaxed.

### Stage 2 — Rebuild the image chain (Tier 2)

#### 2.0 — EXECUTION CORRECTIONS, found 2026-09-22 while preparing this stage

Read these before doing 2.1–2.7. Four of the counts in the original Tier 2/3 analysis were low, and
two are load-bearing rather than cosmetic.

**C1 — `pipeline-lib.sh` IS a task site. The "Not a task" list was wrong about it.**
`pipeline_validate_compose_bind_sources()` at `code/01_platform/04_scripts/pipeline-lib.sh:156–170`
is the **B7 compose bind-source guard**: it asserts every file-mounted compose bind source is a
regular file, so a missing jar fails locally instead of as an opaque OCI `not a directory` at
container start. It carries a **7-entry list that mirrors the compose mounts verbatim**, including
6 versioned jar names and the SNAPSHOT. It must move in lockstep with the compose or the guard
starts rejecting a valid compose (or accepting a stale one). The earlier "Not a task" verdict came
from checking a *different* function in the same file (the L129–148 Fluss-class exclusion), which
genuinely does not need changing.

**C2 — the SNAPSHOT appears at 9 code sites, not 4.** Complete inventory:

| # | Site | Line |
|---|---|---|
| 1–4 | `docker-compose.yml` — coordinator / tablet / jobmanager / taskmanager | 348, 468, 606, 722 |
| 5 | `pipeline-lib.sh` — B7 guard list | 169 |
| 6 | `stage-soak-e2e.sh` — `gate_wire_probe_filesystem` jar loop | 277 |
| 7 | `tests/test_fluss_probes.py` — **asserts the name is present** | 679 |
| 8 | `tests/test_fluss_runtime_image.py` | 233 |
| 9 | `tests/test_flink_runtime_image.py` | 274 |

Sites 7–9 are tests that *assert* the SNAPSHOT's presence or 404-ness. Dropping the jar **breaks**
them, so they must be **rewritten** to assert its absence (the pattern the plan already calls for at
`test_fluss_runtime_image.py:233`), never deleted.

**C3 — the compose line numbers in this plan are now stale, because this plan's own comment edits
moved them.** The `3.3.x`→`3.4.3` comment fix added ~8 lines to each of the two timeout blocks, so
the mount lines shifted: taskmanager `701–706` → **`720–722`**, jobmanager `585–590` → **`604–606`**,
tablet `458–460` → **`466–468`**. Coordinator `346–348` was unaffected. Re-derive line numbers with
`grep -n` before editing; do not trust the numbers recorded above them.

**C4 — the host plugin jars must be re-staged, and the old ones will otherwise silently rot.**
`fluss-plugins/iceberg/` is the dev-compose bind source and is **gitignored** (`.gitignore:6` matches
`*.jar`); only its `README.md` is tracked. The directory currently holds all 7 jars under **0.9.1
names**. Since the compose mounts those paths by name, bumping the compose without re-staging leaves
Compose creating empty directories on the new paths — precisely the OCI failure B7 exists to catch.
Stage the 1.0.0 jars from the already-verified fetch trees (or re-run `fetch-jars.sh --dest`) and
**delete the superseded 0.9.1 jars at the same time**, because a stale 0.9.1 jar sitting beside a
1.0.0 one is how the wrong artifact gets mounted later. The compat jar is the exception — verified
byte-identical, so it is reused, not re-derived.

**C5 — `ddl-apply/Dockerfile`'s `ARG EXPECT_FLUSS` default is load-bearing, not documentation.**
`make ddl-image` invokes `docker compose build ddl-apply` and passes **no `--build-arg`**, so the
Dockerfile default is the value the P4-158 4-way guard actually compares against `code/pom.xml`.
That guard is therefore **already failing right now**, since 1.1 set `pom.xml` to `1.0.0` while the
ARG still reads `0.9.1-incubating`. This is the guard working as designed (a loud, named failure
instead of a confusing "file not found" from the hardcoded COPY path beneath it) — but it means
`make ddl-image` is red until 2.6 lands.

2.1 `fluss-runtime/fetch-jars.sh`: `FLUSS_VERSION`, and the 2-entry `PINS` table with re-recorded
SHA256s. Update the header's SNAPSHOT-exclusion rationale (see 2.3).

2.2 `flink-runtime/fetch-jars.sh`: `FLUSS_VERSION` and the 6-entry `PINS` table. **The `s3` and
`hdfs` SHA256s in this file must equal the ones in 2.1** — that equality is the anti-drift control.

2.3 Drop `fluss-fs-hadoop-shaded-0.9-SNAPSHOT.jar` at **all 9 code sites listed in C2 above** — the 4
compose mounts, the B7 guard list, the soak-gate jar loop, and the 3 tests (which are **rewritten to
assert absence**, not deleted). Doing only the 4 compose lines leaves the B7 guard demanding a file
that no longer exists. The script headers already argue the case (all 12,898 entries are present
across the published s3 + hdfs pair, and the jar declares no `FileSystemPlugin` of its own). 1.0.0
makes this strictly better: `fluss-fs-hadoop:1.0.0` is a properly published artifact, so the
"unpinnable SNAPSHOT" problem disappears rather than being worked around. The 3 remaining `README.md`
and 2 `fetch-jars.sh` header mentions document *why it is excluded* and stay correct — do not
"fix" them.

2.4 `fluss-runtime/Dockerfile`: `FROM apache/fluss:1.0.0@sha256:…` (the full digest, not a truncation)
and the 2 `COPY` lines. **Then verify, do not assume, that the derived image is still needed.** The
Dockerfile's premise is plugin-classloader isolation. **Structural premise CONFIRMED 2026-09-22**
against the extracted official 1.0.0 binary distribution: `plugins/` holds **10 format directories**
(`hdfs hudi iceberg influxdb jmx lance oss paimon prometheus s3`), and `plugins/iceberg/` contains
**only** `fluss-lake-iceberg-1.0.0.jar` while `plugins/s3/` contains **only** `fluss-fs-s3-1.0.0.jar`
and `plugins/hdfs/` **only** `fluss-fs-hdfs-1.0.0.jar`. One jar per directory, so the iceberg plugin's
classloader still cannot resolve `S3AFileSystem` on its own and the overlay copy remains necessary.
**The empirical half — stock half ANSWERED 2026-09-23** (see `T2.16`): the stock 1.0.0 image with the fs-jar
mounts wrote a fresh datalake table to R2 (8 objects / 136,222 B, 0 tiering errors). The derived image is
**not** deleted — stock-alone cannot resolve `s3://` from its plugin layout, so the derived image is the
production carrier of those same two jars. The derived-image run is still owed, and is best made where the
derived image is the serving image: the cutover (`T7.1`).

   > **Side finding — the distribution's plugin jars are NOT byte-identical to Maven Central's, and
   > that is expected.** Comparing `plugins/s3/fluss-fs-s3-1.0.0.jar` from the distribution against
   > the same artifact from Maven Central: identical size (44,689,077), **different SHA256**
   > (`fa75309a…` vs the pinned `5226480e…`). The cause is benign: all **26,773 of 26,773** entries
   > have matching CRC-32, and the `MANIFEST.MF` is identical (`Build-Jdk 11.0.28`,
   > `Specification-Version: 1.0.0`) — only the ZIP entry mtimes differ (`2026-09-18 10:21` for the
   > distribution, `12:15` for Maven). The contents are the same; the archives were re-stamped.
   >
   > Two consequences worth keeping. First, **`fetch-jars.sh`'s design is validated**: pinning the
   > Maven Central bytes and staging those into the derived image produces an image functionally
   > equivalent to the stock one. Second, a naive hash comparison between a distribution jar and a
   > Maven jar **will look alarming and is a false positive** — compare entry CRCs, not file hashes.

2.5 **`hadoop-mapreduce-compat-2.8.5.jar` — largely pre-answered 2026-09-22; `make flink-image`
remains as confirmation only.** The `COMPAT_SHA256` is derived from
`hadoop-mapreduce-client-core-2.8.5` and is independent of Fluss, so it still holds. Its *reason for
existing* was re-tested directly: `org/apache/hadoop/mapreduce/lib/input/FileInputFormat` is absent
from both 1.0 jars, so the class is still needed and the jar is still required. The bundled Hadoop
did move to **3.4.3** (read from the jar's `META-INF/NOTICE`, not inferred), which is why the
derivation's *clash-avoidance* premise still applies — we still strip to
`org/apache/hadoop/mapreduce/**` so the compatibility jar cannot supply a competing
`Configuration`. Re-run `make flink-image` and let `--verify` check `COMPAT_SHA256`; a drift here
would mean the recipe changed and needs the pinned `LC_ALL=C` collation re-applied. The locale trap
is already documented in the script (C collation vs `en_IN.UTF-8` produce different bytes for
identical content).

2.6 `ddl-apply/Dockerfile` — the easy-to-miss one, and per C5 it is already failing. Four sites, not
two: the L52 comment ("the five COPYs below hardcode fluss-client/jackson/slf4j versions"), the
`ARG EXPECT_FLUSS=0.9.1-incubating` default at L53, the L92 comment ("fluss-client is the shaded
0.9.1 artifact"), and the hardcoded `COPY` path at L93–94 (which names `0.9.1-incubating` in **both**
the source and the destination). Note the guard's own message names the four places that must move
together — Dockerfile ARG, `code/pom.xml`, `ddl_apply.py`, `versions.pin` — and 1.1 + 1.3 have
already moved two of them.

2.7 Build all three images: `make fluss-image`, `make flink-image`, and `make ddl-image`. Each runs
its jar staging and verification, so a bad pin fails the build rather than shipping. Also re-stage
`fluss-plugins/iceberg/` per C4, and run `make check-image-stale` so the staleness stamps are
recomputed from the new sources.

#### 2.8 — EXECUTION RESULTS, recorded 2026-09-22 after implementing this stage

**Landed as source edits:** 20 files, +75/−73. `docker-compose.yml` went from **18 → 14** jar
mounts; the whole pre-existing red set cleared.

| Check | Before | After |
|---|---|---|
| `test_flink_runtime_image` | FAILED (failures=2) | **42 tests, OK** |
| `test_fluss_runtime_image` | FAILED (failures=3) | **19 tests, OK** |
| `test_fluss_probes` | FAILED (failures=1, skipped=11) | **35 tests, OK (skipped=11)** |
| `test_compose_form_contract` | FAILED (failures=2) | **5 tests, OK** |
| `test-pipeline-lib.sh` (carries the B7 guard) | — | **134 passed, 0 failed** |
| `docker compose config` (both env files) | — | parses, `rc=0` |
| `make pin-check` | — | **PASS (6/6)** |

##### Correction C6 — 2.6 says "four sites"; there are **three**

The L47–52 comment that 2.6 counts as a site **deliberately names no version**: it says the five
COPYs "hardcode fluss-client/jackson/slf4j versions that MUST match `code/pom.xml`". That is the
correct way to write it, and it needs no edit. The three real sites are the `ARG EXPECT_FLUSS`
default at L53, the L91 comment ("fluss-client is the shaded 1.0.0 artifact"), and the L93–94 COPY
path (both source and destination). The guard's own message names four *places that must agree*
(Dockerfile ARG, `code/pom.xml`, `ddl_apply.py`, `versions.pin`) — that is a different count from
"four sites in this file". `ddl_apply.py`'s only Fluss reference is the comment at L78.

##### Correction C7 — 2.3's justification for dropping the SNAPSHOT is **false**

2.3 asserts: *"1.0.0 makes this strictly better: `fluss-fs-hadoop:1.0.0` is a properly published
artifact, so the 'unpinnable SNAPSHOT' problem disappears rather than being worked around."*

Measured 2026-09-22 against Maven Central:

| Artifact | 1.0.0 | 0.9.1-incubating | Size |
|---|---|---|---|
| `fluss-fs-hadoop-shaded` | **HTTP 404** | **HTTP 404** | — |
| `fluss-fs-hadoop` | HTTP 200 | HTTP 200 | **10,319 bytes** |

`fluss-fs-hadoop-shaded` is **still unpublished in 1.0.0** (`maven-metadata.xml` lists no versions at
all), so the unpinnable problem does **not** disappear. `fluss-fs-hadoop` is not a substitute: its
pom depends on `fluss-fs-hadoop-shaded` and its main sources are 4 files
(`HadoopFileSystem`, `HadoopDataInputStream`, `HadoopDataOutputStream`, `HadoopFileStatus`) — the
10 KB "unrelated stub" `flink-runtime/fetch-jars.sh` already names. The 1.0.0 distribution ships
**no `hadoop` plugin directory** (its 10 are `hdfs hudi iceberg influxdb jmx lance oss paimon
prometheus s3`).

**The correct justification is the one the repo already wrote**, and it is unchanged by 1.0.0:

- all **12,898** entries of the SNAPSHOT are already contained in the published `fluss-fs-s3` +
  `fluss-fs-hdfs` pair (verified entry by entry, 0 missing);
- it declares **no Fluss `FileSystemPlugin` service of its own**, so nothing loads it by name;
- therefore it cannot be pinned *and* is not needed.

Both fs jars carry `org/apache/hadoop/conf/Configuration.class` in **0.9.1 (7,729 hadoop entries)
and 1.0.0 (8,073)** alike — the self-containment is not new in 1.0.0.

**Stronger still: the images already excluded it.** `fetch-jars.sh` never fetched it; only the
**dev compose** bind-mounted the host copy. Dropping the 4 compose mounts therefore *completes a
decision the image side had already made*, rather than introducing a new one.

> **Measurement trap hit here (5th of the session).** An `unzip -l` on a **nonexistent path** prints
> nothing, so a `grep -c` over it returns **0** — which reads as "this jar contains no hadoop
> classes" and briefly produced exactly the false premise above. The path was missing one directory
> component (`fluss-plugins/…` instead of `code/01_platform/01_docker/fluss-plugins/…`). **Always
> stat the file before trusting a count derived from it.**

##### Correction C8 — the first full `make gate` found a regression **this upgrade introduced** (`SC2066`)

Dropping the second jar from the probe loop in `stage-soak-e2e.sh` left a **quoted `for` over a single
item**. shellcheck 0.9.0 raises `SC2066` (**error** severity):

```
stage-soak-e2e.sh line 276:
  for jar in "$plugin_dir/fluss-fs-s3-1.0.0.jar"; do
             ^-- SC2066 (error): Since you double quoted this, it will not word split,
                 and the loop will only run once.
```

The gate stopped at **step 1 of 19**, so the other 18 steps never ran — *a red early step hides every
red behind it*, once more.

**Why earlier verification missed it.** `make static-check` last passed on the **pre-edit** tree
(`b553caf34`). The Stage 2 edit batch landed *after* that run, so this file's shellcheck coverage was
never exercised on the changed tree. **The rule this establishes: re-run the cheapest covering gate on
the post-edit tree, not only on the tree you had before the batch.** `make static-check` takes seconds
and would have caught it immediately.

**Fixed by deleting the dead structure, not by suppressing the warning** — a one-item loop is
structure that no longer means anything:

```bash
jar="$plugin_dir/fluss-fs-s3-1.0.0.jar"
[ -r "$jar" ] || { echo "SOAK-E2E: WARN …" >&2; return 0; }
jars+=("$jar")
```

`jars` stays an array, so both consumers (`GATE_FS_CP` and `basename "${jars[0]}"`) are untouched.

Two constraints from `test_fluss_probes.py` were respected deliberately, and both are easy to violate
while "just adding a comment" here:

- the added comment **must not contain the string `hadoop-shaded`** — `:679` asserts that string is
  absent from this whole file;
- the helper must keep **≥4** `return 0` branches — `:731`. It has 5.

**Verified:** `make static-check` → `static-check: 0 failures`, rc=0 · `test_fluss_probes` →
**35 tests, OK (skipped=11)** · `test-pipeline-lib.sh` → **134 passed, 0 failed**. Bare `shellcheck`
still exits 1 on two pre-existing notices below the gate's threshold (`SC1091` L284, `SC2012` L433).

##### Finding F1 — pre-existing supply-chain drift in a mounted jar (fixed, own commit)

The `hadoop-mapreduce-compat-2.8.5.jar` that compose bind-mounts into `/opt/flink/lib/` was
**`c5d69f9e…`, 895,768 bytes — not the declared pin `c19c414b…`, 882,001 bytes.** Characterised
rather than assumed:

| Property | Mounted (`c5d69f9e…`) | Pinned (`c19c414b…`) |
|---|---|---|
| entries | 534 | 499 |
| shared entries with **different** CRC | — | **0 of 499** |
| only-in-mounted | **35** (`META-INF/`, directory entries) | 0 |
| entry mtimes | real (`2026-08-31 22:04`) | normalised (`1980-01-01`, `DERIVE_MTIME`) |

So the mounted jar is a **superset with byte-identical class content**: a different *build recipe*
(kept `META-INF` + directory entries, unpinned collation/mtimes), not different code. **Severity
LOW — reproducibility drift, not a behavioural defect.** The image path was always correct
(`fetch-jars.sh` derives and verifies against `COMPAT_SHA256`); only the host mount was stale. Every
jar in `fluss-plugins/iceberg/` now matches its pin.

##### C2 refinement — only **one** of the three tests needed the assert-inversion

C2 said the three tests "assert its presence → must be rewritten to assert absence". Measured:
`test_fluss_runtime_image.py` and `test_flink_runtime_image.py` **already** guard absence via
`code_lines()` (they strip comments so prose about the SNAPSHOT cannot satisfy them) — they needed
only their `FLUSS_VERSION` / `PINNED_BASE` constants bumped. Only `test_fluss_probes.py:679`
genuinely asserted presence; that is the one inverted to `assertNotIn("hadoop-shaded", …)`.

##### Superseded artifacts: moved, not deleted

`fluss-fs-hadoop-shaded-0.9-SNAPSHOT.jar` is **unrecoverable** (404 on every public repository, a
locally built module), so deleting it would be irreversible. All six superseded 0.9.1 jars plus the
stale compat jar were **moved** to `logs/fluss-1.0-superseded-jars-20260922/` (gitignored). Delete
that directory once 1.0.0 is certified.

##### Behaviour claims re-verified rather than assumed

Three comments made behavioural claims naming 0.9.1. All three were checked against 1.0.0 and all
three still hold, so only their version label was updated:

- `flush_guard.sh` — the unbounded flush await is **unchanged**: `TODO add deliveryTimeoutMs to
  report success or failure on record delivery.` is still present at
  `fluss-client/.../write/RecordAccumulator.java:149` (was `:116`).
- `FlussPrefixReader.java` — `Lookuper` is **still not `AutoCloseable`**: `public interface
  Lookuper {` at line 43 in both versions, no `close()`. The P6-731 note stays valid.
- `test_09_stack.py` — the stock image's entrypoint is **unchanged**: 1.0.0's Dockerfile still has
  `ENTRYPOINT ["/docker-entrypoint.sh"]` and `CMD ["help"]`, so the CHG-181 rationale and the
  `command:`-required rule still hold.

**Exit:** both images build; both `--verify` passes report all pinned artifacts matched;
`make images` satisfies gate step [18/19].

### Stage 3 — Offline verification (no cluster)

3.1 `make gate` — all 19 steps. Cheap failures first.

3.2 Edit the Tier 4 test files. **Rewrite `test_fluss_runtime_image.py:233` and
`test_fluss_probes.py:695` to still assert the SNAPSHOT is absent** — that assertion is what keeps the
Stage 2.3 deletion from silently regressing.

3.3 The offline Fluss suites, in order of blast radius:

| Suite | Env gate | What it protects |
|---|---|---|
| `CompatFlussDdlParityIntegrationTest` | `FLUSS_BOOTSTRAP` | DDL parity — the schema contract |
| `CompatFlussCompositeKeyIntegrationTest` + `CompositeKeyMatrixVerifier` | `FLUSS_BOOTSTRAP` | Raw KV key encoding. **Its javadoc asserts 0.9.1's encoding specifically** — that assertion is now version-scoped and must be re-derived, not assumed. |
| `CompatFlussIntegrationTest` | `FLUSS_BOOTSTRAP` | BYTES/LOG/KV/partial_update round-trips |
| `CompatFlinkCheckpointRescaleIntegrationTest` | `COMPUTE_INT_TEST_COMPAT_FLINK` | Checkpoint → restore at 2× parallelism; serializer-change rejection |
| `COMPUTE_INT_TEST_SAVEPOINT`, `COMPUTE_INT_TEST_SIG_STATE_RESTORE`, `COMPUTE_INT_TEST_DEDUP_ROCKSDB` | — | Savepoint and state-restore compatibility |

3.4 Confirm the DDL manifest stays readable across the version boundary. `DdlApplyTool:1478` stamps
`FLUSS_VERSION` into the manifest; check that a 1.0-stamped manifest does not fail the existing gate
and vice versa.

**Exit:** `make gate` green; the rewritten guards pass; all offline suites above green against 1.0
libraries.

### Stage 4 — Scratch-cluster re-certification

**Stand up a parallel 1.0 cluster under a separate compose project name. Do not certify against the
live stack** — Stage 5's first trap cannot be previewed with a config change, only by running.

4.1 Re-record, each with a fresh date and the 1.0.0 versions, in this order:
`COMPAT-FLUSS-005`, `COMPAT-FLUSS-006`, `COMPAT-FLINK-001`, `VM-FLUSS-CLI-006`, `VM-FLUSS-CONN-007`.
Re-record the 7 JAR/image digests in `versions.pin` and the `VM-IMAGES-012` digest/SBOM row.

4.2 Live-gated suites, on a **scratch table, not `raw_table_1`**:

- Ingestion: `INGESTION_INT_TEST_FLUSS`, `_INSTRUMENTS`, `_MANIFEST`, `_PERF`, `_E`
- Compute: `COMPUTE_INT_TEST_TABLET_KILL`, `_TM_KILL`, `_SAFETY`, `_SIGNAL_KV`, `_DEDUP_ROCKSDB`
- Gateway: `GatewayFlussIntegrationTest`, `GatewayFlussDurableReplayIntegrationTest`,
  `FlussProjectionWriterIntegrationTest`
- Common: `FlussGateAttemptStoresIntegrationTest`, `GateMergeEngineDrillIntegrationTest`,
  `FlussPositionsStateStoreIntegrationTest`, `FlussPostbackQuarantineStoreIntegrationTest`,
  `EodBucketCopyIntegrationTest`

4.3 Confirm the ACL and AWS-credential traps are genuinely non-events, and record *how* that was
established rather than asserting it. Both were swept on 2026-09-22 and found absent — no
`createAcls`/`dropAcls`/`sys.add_acl`/`sys.drop_acl` anywhere, and S3 access via static
`fs.s3a.access.key`/`secret.key`/`endpoint`/`path.style.access`, which the upgrade notes explicitly
exempt from the SDK v2 migration. Re-confirm on the bumped tree; if a principal is added later that
manages ACLs, the `ALL`-permission requirement applies from that point.

**Exit:** five evidence records re-issued against 1.0.0; all live suites green.

### Stage 5 — The four behaviour changes

Each is verified by measurement on the scratch cluster. None is inferred from reading.

**B1 — new-partition startup position. The sharpest risk in this upgrade.**

`FlinkSourceEnumerator.java` at `v1.0.0` L610 sets
`newDiscoveryOffsetsInitializer = OffsetsInitializer.earliest()`, documented at L165–167 as FLIP-288
semantics "to prevent data loss". **There is no configuration option** — the field is assigned once
in the constructor and used at L1055 for log-table partitions discovered after startup. At
`v0.9.1-incubating` the field does not exist and new partitions followed the job's
`scan.startup.mode`.

This lands directly on `raw_table_1`, because DDL v3 deliberately routes late ticks into old
`event_day` partitions. A reactivated old partition therefore replays from its beginning, mid-job,
without warning. Expected symptom if unhandled: a burst of stale candles and duplicated signal
intents.

Tasks:

- Measure how often a late tick actually lands in a partition older than the current day. This
  determines whether B1 is a scheduling note or a blocking defect, and it is answerable from existing
  `raw_table_1` data.
- Reproduce on the scratch cluster: write to an old `event_day` partition while a 1.0 source is
  running, and observe the seek.
- Decide the mitigation and record it: accept it (and bound the duplicate window), or guard the source
  (explicit offset tracking per partition instead of relying on discovery semantics).

**B2 — removed metrics.** `fluss.reader.bucket.<n>.currentOffset` gauges are gone. Grep the
observability config, dashboards, and `fluss-client-metrics.py` before cutover; the failure mode is
silence, not an error.

**B3 — `max.bucket.num` default `128000 → 4096`.** Check whether any of the 42-row perf baseline runs
were bucket-count sensitive before comparing to Stage 6's numbers.

**B4 — `remote.data.dirs` is ADDITIVE, not a rename. Zero exposure unless we adopt it.**
**Downgraded from "behaviour change to verify" to "no action" on 2026-09-22.** The original wording
("`remote.data.dir` → `remote.data.dirs`") read like a rename, which overstated it. The 1.0 source
settles it — `fluss-common/.../config/ConfigOptions.java`:

- `REMOTE_DATA_DIR` (`remote.data.dir`) at **L113–122** — **still present in 1.0**, declared
  `.noDefaultValue()`, **not removed and not deprecated**.
- `REMOTE_DATA_DIRS` (`remote.data.dirs`) at **L124+** — new in 1.0, a `List<String>`,
  `ROUND_ROBIN` selection via `remote.data.dirs.strategy`.
- The 1.0 javadoc states the precedence itself: *"When upgrading to `remote.data.dirs`, please ensure
  this value is placed as the first entry in the new configuration. For new clusters, it is
  recommended to use `remote.data.dirs` instead. **If `remote.data.dirs` is configured, this value
  will be ignored.**"*
- 0.9.1 has only `remote.data.dir` (`ConfigOptions.java:95`), so nothing is lost by staying on it.

**What we actually configure:** the singular key, in **7 places** — `docker-compose.yml:393` (coordinator)
and `:507` (tablet), plus `docker-stack.yml:335, 423, 498, 573` (4 production services) — all set to
`s3://${R2_BUCKET}/remote-data`. Because `remote.data.dirs` is unset everywhere, the old key is read
exactly as before and **no existing R2 data becomes unreachable**. The migration trap is real but only
arms if someone *chooses* to adopt `dirs`; the ordering requirement is documented above so that choice
is not made blind later.

Note also that 1.0.0's shipped default `conf/server.yaml` still uses `remote.data.dir` — consistent
with the key being supported rather than retired.

**Stage 5 keeps a real R2 read under B4, but as a general migration check, not as a B4-specific
defect hunt** — the point is now "the upgraded server still reads what the old one wrote", which is
worth proving once regardless.

**B5 — NEW, found 2026-09-22 by running the stack: 1.0 REJECTS writes to an out-of-date auto-partition. 0.9.1 accepted them.**

This behaviour change was **not in the four** this stage was built around; it was found by bringing the dev stack
up on 1.0.0 and reading the ingestion container's exit. It is the most operationally visible change in the
upgrade so far, because it converts previously-accepted data into a hard fail-fast.

**The rule exists only in 1.0.** `grep -rn "out-of-date"` across each whole source tree:

| tree | files matching |
|---|---|
| `v0.9.1-incubating` | **0** (of 2,908 Java files) |
| `v1.0.0` | **5** — `PartitionUtils` (the rule), `CoordinatorService` (the caller), 3 ITCases |

**Where it lives.** `fluss-common/.../PartitionUtils.java` grew **225 → 620 lines**. 1.0 adds
`validateAutoPartitionTime` (L128) and `isPastAutoPartition` (L177); the throw is at **L164–170**:

```java
String lastRetainPartitionTime = generateAutoPartitionTime(currentZonedDateTime,
        -autoPartitionStrategy.numToRetain(), timeUnit, autoPartitionStrategy);
if (lastRetainPartitionTime.compareTo(partitionTime) > 0) {
    throw new InvalidPartitionException(String.format(
        "Partition value '%s' is out-of-date. The earliest retained partition is '%s'.",
        partitionTime, lastRetainPartitionTime));
}
```

**A new call site, not just a new helper.** `CoordinatorService.java` (1.0) calls it explicitly after the spec
check — a call that has no 0.9.1 counterpart (both versions call `validatePartitionSpec` the same number of
times, 3; only 1.0 adds this second step):

```java
validatePartitionSpec(tablePath, tableInfo.getPartitionKeys(), partitionSpec, true);
// second, check whether the partition is out-of-date.
validateAutoPartitionTime(partitionSpec, tableInfo.getPartitionKeys(),
        tableInfo.getTableConfig().getAutoPartitionStrategy());
```

Old `validatePartitionSpec` validated **only** spec size, key presence and value format. There was no
retention/time rule anywhere in 0.9.1.

**Observed effect, immediately reproducible on our own stack.** With the stack up on 1.0.0 the ingestion
container fail-fasts and exits 1 (three times, then stops — `on-failure:3` per CHG-117):

```
raw-writer: FATAL append error (table=raw_table_1, class=InvalidPartitionException)
ingestion: FATAL append: FATAL: Partition value '19700104' is out-of-date.
           The earliest retained partition is '20260913'.
ingestion: exiting nonzero after FAIL-FAST (...) — container restart policy revives us
```

`raw_table_1` retains ~10 days, so any `event_day` older than `20260913` is now refused. The offending
partition comes from the **faketool's synthetic ticks**, not from real market data: the same log shows
`proto tick processing error: VALID_TRADE requires lastPricePaise > 0` immediately beforehand, i.e. a garbage
tick carrying a zeroed timestamp reaches `EventDay.ofValidated(packet.eventTime())` and yields `19700104`.

**Disposition — a decision, and it is not "add a config knob" (there is none).** Options:

1. **Teach the ingestion to drop ticks whose `event_day` predates the retention floor**, treating it like the
   other per-tick validation errors (`errors=41` are already tolerated; this one is fatal only because the
   *server* rejects the append). This is the principled fix: bad data should be dropped, not crash a service.
2. **Fix the synthetic data source** (the faketool's zeroed timestamp) so the fixture stops emitting 1970 dates.
   Cheaper, but only helps the test path — a real feed glitch would still crash production.
3. **Turn off auto-partitioning / raise `numToRetain`** — wrong: disables a data-loss guard to accommodate
   bad input.

**Recommendation: (1), and additionally (2) so the fixture is honest.** The upgrade made the server *stricter*,
which is correct — a 1970 partition in a 10-day table is nonsense the pipeline should never have written. The
defect this exposes is on our side: the ingestion treats a server-side data rejection as an unrecoverable
process fault instead of a dropped record. **This is worth a change record of its own**, and it is the strongest
argument in the whole upgrade that the pipeline's error taxonomy needs a "server rejected this record" class.

**Exit:** B1 additionally has a
mitigation decision with its reasoning.

### Stage 6 — Performance re-measurement

Record beside the 0.9.1 numbers; do not overwrite them.

6.1 Writer tuning p99 and the 50k ticks/s baseline, same harness (`INGESTION_INT_TEST_PERF`).

6.2 **The A2b-v3 disk-saturation signature**: 150–200 MB/s, ~90 s bursts, 35/42 aligned across 3 runs.
If it repeats on 1.0, that is a finding, not a baseline. 1.0 ships the native fix
(`server.data-disk.write-limit-ratio`, `write-recover-ratio`, `check-interval`) — measure with the
throttle at default first, then configured, and record both.

6.3 **Fetch latency, local vs remote.** This is where `client.scanner.log.read-preference` pays off and
it is the measurement that justifies the whole exercise: `local-first` (default) vs `remote-first`
against our real access pattern, plus `client.remote-file.download-thread-num`.

6.4 KV recovery time, first with `table.kv.standby-replica.enabled` **off**, then on. It is disabled
for pre-existing tables and requires an explicit `ALTER TABLE`; it is failover speed, not durability.

6.5 **Do not enable `log.retention.roll-active-segment.enabled` during the upgrade.** The upgrade
notes require it to stay disabled for the entire rolling upgrade — which is the default and needs no
action — and to be enabled only afterwards via
`CALL sys.set_cluster_configs(config_pairs => 'log.retention.roll-active-segment.enabled', 'true')`.

**Exit:** p99, throughput, disk signature, fetch latency (both modes), and KV recovery time recorded
as 1.0 numbers, with the 0.9.1 values still present for comparison.

### Stage 7 — FIP-27 (D2)

Executed as documented, and the "non-rollbackable" framing is narrower than it first appears:

- The clean layout applies to **newly created** lake tables only.
- Existing legacy tables are **not migrated** — the notes state they "remain fully readable and
  writable", and the layout is detected from the physical schema so both coexist indefinitely.
- New readers read **both** layouts.

So the only dangerous combination is an **old** reader facing a **new clean** table, plus an old
tiering service touching a clean table. Our exposure today is zero: the dev cluster runs
`table.datalake.enabled=false` (asserted in `CompatFlussDdlParityIntegrationTest:233–235`), so no lake
tables are being created.

Tasks, in the required order:

1. **Lake-reading Flink connectors and lake storage plugins** — zero-risk, needs no coordination.
2. **Tiering service** — restart it only after readers are on 1.0.
3. **Fluss cluster.**

Then: **create no new lake table until every reader is on 1.0.** That single scheduling rule is what
converts the rollback hazard into a sequencing note.

Also in this stage: `datalake.enabled`. If `datalake.format` is set and `datalake.enabled` is left
unset, legacy behaviour is preserved and nothing breaks. If the cluster is later moved to explicit
`datalake.enabled`, the notes specify the pairing rules — `true` requires a non-empty
`datalake.format`; `false` pre-binds a format without activating lakehouse tables.

**Exit:** readers on 1.0, tiering on 1.0, cluster on 1.0, zero clean tables created, rollback still
available.

### Stage 8 — Docs, evidence, change record (Tier 5)

8.1 Write **`docs/05_deployment/change-records/CHG-299.md`** in the `CHG-297` format. Fill in every
field, and get these three right:

- `compatibility_class` — `COMPATIBLE_WITH_LIMITATION`, citing B1 (new-partition startup is
  hardcoded to earliest, with no override) and any other open item from Stage 5.
- `savepoint_impact` — Flink state and Fluss state are not touched by the pin bump, but Stage 3.3's
  savepoint suites are what prove that; cite them.
- `rollback_behavior` — name what has **no** rollback. After any clean lake table exists, an older
  tiering service or older reader cannot be restored. Before that, rollback is a pin revert.

8.2 **Do not edit any of the 23 frozen change records or `CHG-298`.** History is append-only.

8.3 Tier 5 sweep: the 5 Java comment sites, `flush_guard.sh`, the three image READMEs, the probe
README, `hadoop-conf/core-site.xml`, `compute/README.md`, `ingestion/pom.xml:121`.

8.4 Move `AC-FLS-017` from `NOT_IMPLEMENTED` to implemented, citing `table.kv.ttl` and
`table.kv.ttl.time-column`, and correct the three storage docs that assert per-key TTL does not exist
(`docs/02_requirements/02-functional/02-storage.md:200`, `docs/04_contracts/02-storage.md:56`,
`docs/03_architecture/00-arch-overview.md:187`).

8.5 Revisit the decisions this upgrade touches. `DEC-040` was written on the premise that
`client.writer.retries` defaults to `Integer.MAX_VALUE`; that is **still true in 1.0.0**, so
`DEC-040` survives — but its reasoning should cite 1.0.0 so the next reader does not have to re-check.
Same for `ASM-NFR-006`, `ASM-IF-002`, `ASM-IF-004`, and `docs/ENVIRONMENT.md` `FACT-011` / `FACT-014`
(the derived-image rationale).

8.6 Update the 8 `docs/08_implementation/*.md` files, and `01_project/04-decisions.md`,
`01_project/05-risks-and-assumptions.md`, `02_requirements/05-interfaces.md`,
`03_architecture/01-technology-choices.md`, `04_contracts/01-ingestion.md`.

**Exit:** `make full-audit` green; `CHG-299` written; frozen records byte-identical.

**Exit met 2026-09-22 — the certifying gate has run.** `GATE RESULT: PASS — 19/19 verified, 0 skipped` on a frozen tree at `1d3c2c9a`, 28.1 min, in `logs/soak/monday-gates-20260922-223433/` (`CHG-302`). It is the first clean end-to-end timing after the optimization and it replaces the ~29.8 min projection with a measurement: **83.4 → 28.1 min (2.97×)**. Two things it does **not** certify, recorded so they are not read into it: tiering to R2 on 1.0.0 (the run used `remote.log.task-interval-duration=0s`, FACT-018, so tiering was off — `FACT-011` outstanding), and any native 1.0.0 capability (`DEC-053` sequences those after the certificate).

### Stage 9 — Production cutover (D3)

9.1 **Decide the production pin here, not earlier.** Weigh Stage 6's numbers and how much time has
passed since 2026-09-18. `release-1.0` exists, so 1.0.1 would be a cherry-pick rather than a rebase.

9.2 Push the images, then resolve the manifest digest with `digest-pin.sh <repo>:<tag>` — a **pushed**
manifest digest, not a local image ID and not the stock tag. Update `runtime.lock`, the Swarm
environment's `FLUSS_IMAGE`, and re-run `make pin-check` (its check [5/6] requires exactly 64 hex).

9.3 `make images`, then gate step [18/19] for content stamps and staleness.

9.4 Nothing else in `docker-stack.yml` changes — it has zero jar bind mounts and takes the version
entirely through `FLUSS_IMAGE`.

**Exit:** production runs a pushed, digest-pinned 1.0 image; the whole gate is green on the cutover
commit; `CHG-299` records the final pin.

---

## Verification matrix

| Claim | How it is proven | Stage |
|---|---|---|
| Every jar in both images is the published 1.0.0 artifact | `fetch-jars.sh --verify` against freshly recorded SHA256s | 2.7 |
| The derived runtime image is still required (structure) | `plugins/iceberg/`, `plugins/s3/`, `plugins/hdfs/` each hold exactly one jar in the 1.0.0 binary dist | **DONE 2026-09-22** |
| The derived runtime image is still required (behaviour) | Tiering write against stock 1.0 image, then derived | 2.4 |
| The compat jar is still required under 1.0 | `FileInputFormat` absent from both 1.0 jars; wrapped Hadoop = 3.4.3 per jar NOTICE | **DONE 2026-09-22** |
| The compat jar still derives to the pinned bytes | `make flink-image` + `COMPAT_SHA256` verify | 2.5 |
| S3A socket-timeout mapping survived SDK v1→v2 | `AWSClientConfig` still reads both keys; `fs.s3a.socket.timeout` absent from jar | **DONE 2026-09-22** |
| The R2 timeouts still bound a wedged socket in practice | Real R2 read under a stalled connection | 5 B4 |
| P1-052's shader exclusion is still correct | `unzip -l` on `fluss-client-1.0.0.jar` for `org/slf4j/` | 1.6 |
| DDL parity and composite-key encoding hold | `CompatFluss*` integration suites | 3.3 |
| Savepoints restore across the version boundary | `COMPUTE_INT_TEST_SAVEPOINT`, `_SIG_STATE_RESTORE` | 3.3 |
| Client, connector, and cluster interoperate | 5 re-recorded `VM-*` / `COMPAT-*` evidence records | 4.1 |
| New partitions start at earliest, and the impact is bounded | Reproduced on scratch; frequency measured from `raw_table_1` | 5 B1 |
| No dashboard depends on a removed gauge | Config grep + dashboard read | 5 B2 |
| Bucket-count default change is inert for us | Perf run comparison | 5 B3 |
| `remote.data.dir` still reads existing R2 data | Source: key retained at `ConfigOptions.java:113`; we set it in 7 places; `dirs` unset | **DONE 2026-09-22 (no exposure)** |
| The upgraded server reads what 0.9.1 wrote to R2 | Real read against R2 | 5 B4 (general migration check) |
| 1.0 is not slower, and fetch latency improves | Re-measured p99, throughput, disk signature, both read preferences | 6 |
| The lake path can still roll back | Zero clean tables created | 7 |

## Risks

| Risk | Severity | Handling |
|---|---|---|
| **B1 — silent partition replay on `raw_table_1`** | High | Stage 5, on a scratch cluster, before the live `SignalJob` moves. Hardcoded, no knob, so it cannot be configured away. |
| **The 7 uncommitted patches are lost** | High (irreversible) | Stage 0 commits the branch **before** creating the worktree. |
| **A stale SHA256 silently pins an old jar** | High | Every digest re-recorded from the published artifact, and `--verify` fails the build on mismatch. |
| **`hadoop-mapreduce-compat` becomes wrong under Hadoop 3.4.3** | Medium | Stage 2.5 re-derives rather than carries the pin forward. |
| **P1-052 exclusion is stale** | Medium | Stage 1.6 re-runs the `unzip -l` check. |
| **A FIP-27 clean table is created early, ending rollback** | Medium | Stage 7's one-line rule: no new lake table until all readers are on 1.0. **Concrete trigger now identified:** A4's recreate of `Safety_Halt_Requests` would do exactly this, because its DDL declares `table.datalake.enabled=true` (`code/01_platform/02_sql/ddl/18_safety_halt_requests.sql:78`). `T9.5` must therefore either set it false on the recreate or wait for the reader migration. |
| **1.0.0 is ~4 days old** | Medium | Stage 9 is the decision point; certification work is identical for 1.0.1. |
| **A host reboot leaves the dev stack half-up** | Low (dev only) | `dockerd` can start before the ecryptfs mount behind `/home/saurabh`, so the eight jar-bind containers fail (`not a directory`) and no restart policy retries them. Remedy: re-run `code/01_platform/04_scripts/stack-restore-verify.sh` and require `RESTORE VERDICT: OK`. Production mounts no Fluss jars (`T9.4`), so it cannot fail this way. |
| **A missing edit in one of the 34 files** | Low | The measured surface is in this document; Stages 1–4 each end on a gate. |

## Rollback

**Stages 1–6: rollback is a pin revert** and a rebuild. Restore `fluss.version`, both `fetch-jars.sh`
PINS tables, the Dockerfiles, and `runtime.lock`, then `make images`. Cheap, and it is the reason the
stages are ordered so that nothing irreversible happens before Stage 7.

**After Stage 7, once any clean lake table exists: no rollback.** An older tiering service or older
lake-reading connector cannot correctly read or write a clean table, and a clean table cannot be
transparently returned to the legacy layout. The control is the Stage 7 rule, not a procedure.

**The production cutover (Stage 9) is a digest swap** and is reversible by restoring the previous
digest — until a clean lake table exists.

## Ordered task list

**Superseded 2026-09-22.** This section was prose summary; the atomic, marked task list now lives in
**§0 Live tracker** above and is the single source of truth. The ten-line stage ordering it used to
carry is preserved in §0's stage grouping (Stage 0 → Stage 9), with the same sequence.
